# Dungeon Legion — 저장소 안내

Three.js r128으로 만든 아이소메트릭 던전 크롤러 RPG. **`index.html` 단일 파일**(약 1.8MB,
18,000줄 남짓)이 게임 전부다. GitHub Pages가 `main`에서 배포하므로 `main`에 올라가는 순간
라이브다. 현재 CrazyGames 제출을 준비 중이다.

## 파일 구조 — 먼저 알아야 할 것

`index.html` 한 파일에 전부 들어 있고, 앞부분은 **동봉된 라이브러리(미니파이)** 다.
전체를 grep하면 three.js 내부가 쏟아진다. 게임 코드만 보려면 4076줄 이후로 한정할 것.

| 줄 | 내용 |
|---|---|
| ~411 | 스타일·UI 마크업 |
| 434 | three.js r128 (미니파이 동봉 — CDN 의존 제거, 건드리지 말 것) |
| 444 | GLTFLoader r128 (동봉) |
| **4076~** | **게임 코드 시작** |
| 4143 | 플래그 정의 블록 (`AD_TEST`/`DEV_MODE`/`PX_MODE`/`GAME_VERSION`) |

```bash
# 게임 코드만 검색하는 방법
awk 'NR>4076' index.html | grep -n "찾을것"
```

## 릴리스 절차

`GAME_VERSION`(4155줄 부근)을 올리는 것이 릴리스다. 좌측 상단 배지에 표시되고,
사용자가 배포 확인에 쓴다. **코드를 바꿨으면 반드시 같이 올린다.**

```js
const GAME_VERSION = 'v685';
```

푸시 후 확인: `git show origin/main:index.html | grep -o "GAME_VERSION = 'v[0-9]*'"`

**작업은 작업 브랜치(`claude/…`)에서 한다. 검증 결과를 보고한 뒤, 사용자가 머지하라고 할 때만
`main`에 머지한다 — 문서 변경도 예외 없다. 임의로 `main`에 올리지 말 것**(사용자 지시).
`main`에 올라가는 순간 라이브이므로, 머지 전에 최소한 게임 스크립트 문법은 통과시킬 것 —
마지막 `<script>` 블록만 잘라 `node --check`:

```bash
S=$(grep -n '^<script>$' index.html | tail -1 | cut -d: -f1)
E=$(grep -n '^</script>$' index.html | tail -1 | cut -d: -f1)
sed -n "$((S+1)),$((E-1))p" index.html > /tmp/gc.js && node --check /tmp/gc.js
```

문서만 고쳤을 때는 `GAME_VERSION`을 올리지 않는다(배지는 게임 코드 배포 확인용).

**게임 파일을 사용자에게 보낼 때는 항상 두 벌로 보낸다**(사용자 지시) — 내용은 같고 이름만 다르다.

| 파일명 | 용도 |
|---|---|
| `index.html` | 그대로 갖다 쓰는 배포용 |
| `index-vNNN.html` | 버전이 이름에 보이는 보관용 |

새 기능은 URL 플래그로 끌 수 있게 만들되, 보내는 파일에는 **켜진 상태로** 넣는다.
사용자가 모바일에서 URL 파라미터를 붙이지 않고 바로 확인할 수 있어야 한다.

## URL 플래그

| 플래그 | 기본 | 용도 |
|---|---|---|
| `?px=0` | px 켜짐 | 픽셀아트 렌더 모드 끄기(예전 룩) |
| `?dev=1` | 꺼짐 | 보스/만렙 테스트 버튼 노출, 셰이더 에러 로그 |
| `?adtest=1` | 꺼짐 | 광고 테스트(dev 포함) |
| `?blob=0` | 블롭 켜짐 | 발밑 블롭 그림자 끄기 |
| `?blobsteps=N` | 3 | 블롭 그림자 계단 수(0=부드러운 그라디언트) |
| `?toon=N` | 0 | 조명 계단화. **기본 꺼짐 — 아래 참고** |
| `?dspec=N` | 2.0 | 깊이 기반 스펙큘러(가까이 강조·멀리 감쇠). 0=끔 |
| `?dlight=N` | 0.6 | 깊이 기반 직접광 감쇠(멀수록 어둡게). 0=끔 |
| `?dnear=` / `?dfar=` | 20 / 33 | 위 두 효과의 깊이 램프 구간(월드 단위, 실측 기반) |
| `?drough=N` | 1.2 | 러프니스 기반 하이라이트(매끈=강조·거침=감쇠). 0=끔 |
| `?droughlo=` / `?droughhi=` | 0.40 / 0.95 | 위 효과의 러프니스 램프 구간(하단 0.40 이유는 아래 참고) |
| `?spec=N` | 0.85 | 하이라이트 전체 밝기 배수(대비는 유지). 1=보정 없음 |
| `?srough=N` | 1.0 | 바위·돌 재질 러프니스(벽·바닥·기둥·잔해·자갈). 0.9=예전 값 |
| `?organic=0` | 켜짐 | 피부(사람·몬스터) 하이라이트·모서리 강조 제거를 끔 |
| `?osmooth=N` | 80 | 피부 노멀 스무딩 각도 한계(도). 0=끔, 180=모서리까지 전부 뭉갬 |
| `?pgrass=0` | 픽셀 잔디 | 마을·던전 잔디를 예전 3D 풀잎 메시로 되돌림 |
| `?pgfull=0` | 풀 빌보드 | 잔디를 Y축 빌보드(월드 수직)로. 스프라이트가 세로로 눌려 보인다 |
| `?ptrees=0` | 픽셀 나무 | 마을 나무를 예전 20면체 덩어리 나무로 되돌림 |
| `?vout=1` | 외곽선 없음 | 식생(잔디·나뭇잎)에도 검은 외곽선을 다시 그린다 |
| `?wrock=0` | 바위 충돌 켜짐 | 1막 벽에 박힌 바위 충돌체 끄기(예전처럼 통과) |
| `?wrockmax=N` | 0.12 | 위 바위가 1칸 복도·문간으로 침범할 수 있는 최대 거리(TILE 배수) |
| `?wrockroom=N` | 0.42 | 같은 값의 방 안쪽 버전(트인 쪽은 더 깊이 막아도 된다) |

조명 보정을 통째로 끄려면 `?dspec=0&dlight=0&drough=0&spec=1` (전부 중립이어야 청크를 안 건드린다).

일반 접속에서 개발자 버튼은 숨겨진다(CrazyGames 요구사항). 제목을 3초 안에 5번 탭하면 나온다.

## 렌더 변경을 비교할 때 — 이거 안 지키면 틀린 결론이 나온다

스크린샷 A/B를 찍기 전에 **rAF 루프를 끊고, 설정을 바꾸지 않은 대조군 한 쌍을 먼저 찍는다.**
대조군 두 장이 픽셀 단위로 동일할 때만 그 다음 측정을 믿는다.

```js
window.requestAnimationFrame = () => 0;   // 루프 정지
renderWithPost(scene);                    // 이후 렌더는 직접 호출
```

실제로 이걸 안 해서 "화면의 11%가 12/255 이상 변한다"는 결과가 나온 적이 있는데,
전부 반짝이 파티클이 움직인 것이었다. 제대로 재니 최대 9/255였다.
`Math.random`도 시드 고정할 것(장면 생성이 난수에 의존한다) — **단, 아래 시점 규칙을 지켜야 한다.**

테스트는 Playwright + 동봉 Chromium:
`chromium.launch({ args: ['--enable-unsafe-swiftshader','--use-gl=swiftshader','--no-sandbox'] })`

npm의 playwright 버전과 동봉 브라우저 빌드 번호가 어긋나면 `Executable doesn't exist …`로 죽는다.
이때 `playwright install`을 받지 말고 `executablePath`로 동봉본을 직접 가리킬 것
(경로는 환경마다 다르다 — `ls /opt/pw-browsers` 로 확인).

### 시드는 `addInitScript`로 — 부팅보다 먼저 걸어야 한다

`page.evaluate` 안에서 `Math.random`을 갈아끼우면 **이미 늦다.** 그 시점엔 마을과 부팅 던전이
진짜 난수로 만들어진 뒤고, 그때 생긴 상태가 페이지가 사는 동안 남아 이후 생성 결과를 바꾼다.

```js
await page.addInitScript(() => {
  let s = 12345 >>> 0;
  Math.random = () => { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; };
});
await page.goto(url);   // 이 뒤로는 부팅까지 전부 결정적
```

측정으로 확인한 것:

- `evaluate` 안에서 시드 → 같은 페이지 안에서는 결정적인데 **페이지를 새로 열면 달라진다.**
  대조군끼리도 33층 방 4↔3, 60층 4↔5로 흔들렸다. 이걸 회귀로 오판할 뻔했다.
- `addInitScript`로 시드 → **서로 다른 페이지 로드 두 번이 바이트 단위로 동일.**
  방 개수는 물론 난수 소비 횟수까지 일치한다.
- 부팅 시드만 바꾸고 측정 시드는 고정하면: 던전 격자 해시는 그대로인데 `buildDungeonMesh`의
  난수 소비가 12831↔14357로 바뀐다. 부팅 상태가 이후 생성에 영향을 준다는 직접 증거다.

**THREE 객체를 만들면 그 자체로 난수를 먹는다.** r128의 `generateUUID`가 호출당 `Math.random()`을
4번 쓰고, 모든 `Object3D`·머티리얼·지오메트리 생성자가 이걸 부른다. 그래서 씬에 만드는 객체 수가
달라지면 — 코드 한 줄로 라이트 6개를 덜 만드는 정도만으로도 — 이후 월드 생성 결과가 통째로
바뀐다(실측: 55층 격자 해시 `c0decf18`→`dee17bb8`). 위 "부팅 상태가 이후 생성에 영향을 준다"는
현상의 정체도 이것이다. **객체 개수가 달라지는 변경은 빌드 간 픽셀 A/B가 원천적으로 불가능하므로,
스크린샷 비교 대신 기능을 직접 계측해서 검증할 것.**

**시드가 안 먹는 것처럼 보이면 거는 시점이 늦은 것이다.** `Math.random`을 캡처해 두는 코드는
없다 — 골렘 빌더가 잠깐 바꿔치기했다 되돌리는 곳 한 군데뿐이고, 호출 시점에 잡아서 복원하므로
시드를 지우지 않는다. 월드 생성에 실시간(`Date.now`)이 끼어드는 곳도 이제 없다
(골렘 시드에 있던 `Date.now()`는 v689에서 뺐다 — 아래 참고).

## 반복해서 발목을 잡은 함정들

**TDZ — 두 번 부팅을 죽였다.** 모듈 최상위 코드는 즉시 실행된다. `const`를 그 코드보다
아래에 선언하면 "Cannot access before initialization"으로 게임이 아예 안 뜬다.
플래그·상수는 **쓰는 곳보다 위에** 선언할 것.

**three.js 프로그램 캐시.** `THREE.ShaderChunk.*`를 바꾸고 `needsUpdate`를 줘도 캐시 키에
청크 내용이 안 들어가서 예전 프로그램이 그대로 돌아온다. 런타임 변경 불가 — 컴파일 시점에
굽고 URL 파라미터로 고른 뒤 새로고침해야 한다.

**인스턴스 `onBeforeCompile`이 이긴다.** 이 게임은 풀 바람 등에 자체 `onBeforeCompile`을 쓰는
머티리얼이 많다. 프로토타입에 심으면 770개 중 275개만 걸린다. 공용 ShaderChunk를 고칠 것.

**r128 `WebGLRenderTarget.setSize()`는 `depthTexture`를 안 줄인다.** 리사이즈 핸들러에서
`rt.depthTexture.image.width/height`를 손으로 맞춰야 한다(안 하면 리사이즈 후 외곽선이 깨진다).

**구름 그림자는 평면이 아니라 포스트에서 깊이로 건다.** 예전에는 y=0.14짜리 어두운 평면 한 장이라
그보다 높은 것(건물·나무·캐릭터)은 깊이 검사에서 평면보다 앞이라 그림자가 아예 안 걸렸다
(실측: 바닥 63.8% / 오브젝트 0.5%). 깊이를 `uInvVP`로 월드 좌표로 되돌려 XZ로 구름을 읽으면
전부 걸린다(오브젝트 53.8% / 바닥 23.7% — 같은 구름 무늬가 화면을 가로지른다).
역행렬은 **픽셀 스냅을 풀기 전에** 갱신할 것 — 색·깊이를 찍은 그 카메라여야 한다.
던전·쉼터에서는 `uCloudStr`을 0으로 둬서 셰이더가 통째로 건너뛴다.

**같은 렌더 타깃에 두 번 그리려면 `scene.background`를 꺼야 한다.** `renderer.autoClear = false`로
막았다고 생각해도, `scene.background`가 Color면 three의 `WebGLBackground.render`가 `forceClear`를
켜서 무조건 clear한다. 노멀 패스를 2패스로 나눴다가 2패스가 1패스 결과를 통째로 지워서
모서리 하이라이트가 화면 전체에서 사라졌다(캐릭터를 다 숨겨도 사라져서 원인 찾기가 오래 걸렸다).
2패스 동안 `background = null`로 두고 끝나면 되돌릴 것.

**피부는 노멀 버퍼에서 '빼는' 게 아니라 '표식으로 덮는' 것이다.** 그냥 빼면 그 자리에 뒤에 있는
벽·바닥의 노멀이 드러나고, 그 경계선이 피부 위에 하이라이트로 그려진다(실측: 피부 픽셀의 10.9%,
최대 49/255). 순수 빨강(1,0,0)을 찍어 두고 셰이더에서 걸러야 0%가 된다 — 인코딩된 노멀은
g·b가 -0.7 아래면 r이 ±0.14를 못 넘으므로 이 조합이 정상 노멀과 겹치지 않는다.
표식 대상은 레이어 1로 고른다(게임 코드는 레이어 0만 쓴다). 드로우콜은 늘지 않는다 —
1패스에서 빠진 피부가 2패스로 옮겨갈 뿐이다(실측 5671 → 5671).
레이어는 `_hideNonSolid`가 매 프레임 붙인다(캐릭터 만들 때가 아니라). 1패스에서 빼는 판정과
2패스에서 덮는 판정이 같은 줄에서 나와야 둘이 어긋나지 않기 때문 — 나중에 추가된 메쉬가
빠지면 그 자리에만 구멍이 남아 원인 찾기가 고약하다.

**새 몬스터의 피부: `markOrganic`만 부르면 나머지는 자동이다.** 하이라이트 제거·노멀 스무딩·
노멀 패스 표식이 전부 `userData._organic`/`_skin` 표식을 보고 돌아간다.
`createAdvancedMonster`의 타입 분기에서 만든 `skinMat`은 이미 자동으로 걸린다
(`type !== 'darkknight' && type !== 'golem'`) — **새 타입이 돌·금속이면 이 제외 목록에 넣을 것.**
자동이 아닌 경우는 하나다: 전용 빌더(`buildXxxBody`)가 바깥 `skinMat`을 가리고 자기 재질을
따로 만들 때. 실제로 궁중 마법사·혼령·시종장의 망령이 이렇게 빠져 있었다(v706에서 수정).
전용 빌더를 새로 쓰면 살·뼈 재질에 `markOrganic`을 직접 걸어야 한다.

**픽셀 잔디(pixel-grass)는 조명을 안 받는다.** 인스턴싱 빌보드 + 자체 ShaderMaterial이라
라이트도 톤매핑도 자동으로 안 걸린다. 그대로 넣었더니 캄캄한 동굴에서 잔디만 발광했다.
그래서 두 가지를 붙여 뒀다 — 지우지 말 것.
· 프래그먼트에서 ACES 톤매핑을 직접 계산한다(`renderer.toneMappingExposure`와 같은 값).
  안 하면 잔디만 밝게 떠서 배경에 붙인 스티커처럼 보인다.
· `litNear`/`litMin`으로 플레이어 횃불 기준 거리 제곱 감쇠. 선형(smoothstep)으로 하면
  중간 거리가 너무 밝게 남는다 — 점광원(decay=2)과 같은 모양이어야 자연스럽다.
마을은 넓게 조명되므로 감쇠를 끄고(`litNear:0`) 낮/밤만 반영한다.

**Y축 빌보드는 이 카메라에서 스프라이트를 절반으로 눌러 버린다.** iso 카메라가 수평에서 51.9°
내려다보므로(오프셋 11.07/20/11.07), 월드 수직 1은 화면에서 0.475로 줄어든다. 원본 모듈의 Y축
빌보드(세로축=월드 +Y)를 그대로 쓰면 잎 그림이 세로로 눌려 뭉개진 얼룩처럼 보인다.
가로·세로를 모두 카메라 축에 맞추는 풀 빌보드가 기본이다(`?pgfull=0`으로 예전 방식 비교).
눌림(밟힘)은 `world.y *= ...`가 아니라 잎 축(up) 방향으로 빼야 한다 — 안 그러면 풀 빌보드에서
잎이 짧아지는 대신 기운다.

**잔디는 노멀 패스에서 반드시 빼야 한다**(`userData.fx` → `_hideNonSolid`). `overrideMaterial`이
프래그먼트 `discard`를 없애 버려서, 알파로 모양을 낸 잎이 전부 불투명 사각형으로 찍힌다.

**검은 외곽선 스위치는 노멀 버퍼의 알파다.** 외곽선은 깊이 차이로 그리므로 노멀 표식색(RGB)으로는
못 끈다. 알파를 따로 쓰면 RGB(=진짜 노멀)를 남길 수 있어서 **나뭇잎은 모서리 하이라이트를 유지한 채
검은 선만** 뺄 수 있다. 잔디는 노멀이 의미 없으니 초록 표식(0,1,0)까지 얹어 하이라이트도 뺀다.
식생 패스(3패스)는 `overrideMaterial`을 쓰지 않고 메시마다 자기 표식 재질로 바꿔 그린다 —
잔디는 알파 `discard`가 살아 있어야 잎 모양대로 찍히고 잎은 진짜 노멀이 필요해서, 요구가 서로
다르기 때문이다. 대상이 메시 두어 개뿐이라 재질 교체가 오히려 싸다(실측 추가 비용: 드로우콜 ≤2,
삼각형 +4,458 = 화면 안 잔디 한 번 더).

**덩어리는 잎보다 어두워야 한다.** 같은 밝기로 두면 덩어리가 캐노피처럼 보이고 그 위의 작은 잎이
묻힌다(`canopyBodyDim` 0.72). 밝기는 `aTint`에 곱해 넣는다 — 색 양자화 뒤에 곱해지는 값이라
밴드를 건드리지 않고 한 단 눌러 준다. 덩어리 구는 20면체(detail 0, 20삼각형)보다 저해상도 구가
삼각형당 둥글기가 좋다(6×5 = 48삼각형 / 20면체 detail 1 = 80삼각형).

**잎 사이 빈틈은 카드 수로 메우지 말 것.** 작은 잎으로 캐노피 표면을 다 덮으려면 그루당 카드가
150장 넘게 필요해 삼각형이 몇 배로 뛴다. 가이드 구체를 저폴리 덩어리(`canopyBody`)로 깔면
잎 사이로 지면 대신 캐노피 안쪽이 보인다 — 그루당 삼각형 80개면 된다.
덩어리를 넣으면 **잎의 `castShadow`는 끄고 덩어리만 그림자를 지게 한다**(그림자 패스 하나가
잎 삼각형만큼 통째로 드는데, 실루엣은 거의 같다). 덩어리 인스턴스에는 회전을 주면 안 된다 —
잎 배치는 회전하지 않은 blob 좌표를 쓰므로 어긋난다. jitter는 인스턴스 이동에 넣으면 정확히 맞는다.

**카드 한 장에 잎 여러 장을 겹쳐 굽는다**(`leafBlades`). 카드=잎 한 장이면 캐노피를 채우는 데
카드가 너무 많이 필요하다. 겹친 잎끼리는 ① `aSprig`로 밝기를 어긋내야 한 장처럼 붙지 않고
② z를 조금씩 띄워야 같은 평면에서 깊이 싸움으로 지글거리지 않는다.
잎 다섯 장짜리 큰 카드는 고사리처럼 보이고 삼각형도 168k까지 오른다 — 세 장짜리 작은 카드를
많이 뿌리는 쪽이 낫다(잎당 삼각형 = `leafBlades × leafSeg × 5/3`).

**가늘고 뾰족한 잎(`leafShape:'blade'`)은 방향을 직접 잡아 줘야 한다.** 무작위 Z회전을 그대로 두면
바늘이 사방으로 흩어져 형태가 안 잡힌다. 구체 바깥 방향을 잎 평면에 투영해(`lookAt` 직후 행렬의
열이 잎의 로컬 축) 그쪽으로 돌린다. 명암도 월드 Y가 아니라 잎 길이 방향(`uv.y`)으로 줘야
밑동이 어둡고 끝이 밝은 잔디 명암이 된다. 잎이 길면 캐노피가 야자수처럼 되므로 짧게·촘촘하게.

**잎 모양이 안 보이면 지오메트리가 원이기 때문이다.** 셰이딩만 손봐서는 '테두리 있는 원'까지가
한계다. `ptLeafGeo`가 물결진 다각형(r = 1 + amp·cos(lobes·θ))을 굽는다. 순수 cos이면 봉우리가
뾰족해 별 모양이 되므로 지수 0.55로 봉우리를 넓히고 골을 얕게 한다.
테두리 판정은 반지름이 아니라 `uv.x`(중심 0 → 테두리 1)로 해야 모양이 원이 아니어도 맞는다.
잎당 삼각형이 `leafSides`개라 개수·크기로 균형을 잡을 것(잎당 24분할·32장이면 마을 삼각형이
132k → 181k까지 오른다. 20분할·28장이면 157k).

**캐노피가 '구 여러 개 붙은 덩어리'로 보이면 잎 단위 셰이딩이 없는 것이다.** 명암을 가이드 구체의
바깥 방향으로만 내면 같은 명암대에 있는 이웃 잎끼리 색이 완전히 같아서 한 덩어리로 뭉친다.
잎 로컬 좌표(`vLocal`)로 ① 위는 밝고 아래는 어두운 기울기를 명암대 나누기 '전에' 더하고
② 테두리를 한 단 어둡게 찍으면 잎마다 윤곽이 보인다. 테두리 반지름(`leafRimAt`)을 너무 낮추면
테두리가 두꺼워져 잎이 동전처럼 보인다(0.72는 두껍고 0.84가 적당).
이때 잎의 무작위 Z회전은 빼야 한다 — 로컬 +y가 제각각이면 ①의 방향이 무의미해진다.

**ACES는 밝은 쪽 채도를 눌러서 선명한 색을 못 만든다.** 픽셀아트 레퍼런스의 쨍한 초록(#94ce50)을
목표로 입력색을 역산해 봤더니 어떤 값을 넣어도 #bfce99 같은 뿌연 연두로만 나온다(`aces.js` 역산).
그래서 나뭇잎은 톤매핑을 끄고(`useACES:false`) 적은 색이 그대로 나오게 한다 — 대신 색을 씬 밝기에
맞춰 직접 골라야 한다. 잔디는 지금 톤이 맞으므로 ACES를 켠 채로 둔다.

**픽셀 나무는 잔디와 반대로 노멀 패스에 넣어도 된다.** 잎이 알파가 아니라 육각 지오메트리라
`overrideMaterial`이 `discard`를 지워도 실루엣이 같다. 대신 바람은 반드시 `instanceMatrix`를
CPU에서 갱신할 것 — 정점 셰이더로 흔들면 색 패스만 흔들리고 노멀·그림자 패스는 제자리라
외곽선이 형상과 어긋난다.

**인스턴싱 나무를 마을 씬에 그냥 넣으면 마을 전체가 막힌다.** 마을은 `townScene.children`을
훑어 바운딩 박스로 콜라이더를 자동 등록하는데, 인스턴싱 그룹은 바운딩이 숲 전체다.
`group.userData.noCollide = true`로 자동 등록에서 빼고 그루마다 `_collide`를 직접 부를 것.
콜라이더는 잔디 배치(`buildTownGrass`)보다 먼저 등록돼야 나무 자리에 풀이 안 난다 — 지금 순서가
자동 등록(6771) → 잔디(6804)라서 맞는다.

**셀 배치에 콜라이더 여유를 주면 밀도가 무너진다.** 마을 링의 콜라이더 283개 중 235개가
가로등·통 같은 소품이다. 점으로 뿌리던 예전과 달리 셀은 1.6칸이라 소품 하나가 그만한 구멍을
낸다. 실측 셀 수: 링 전체 964 / 예전 규칙(+0.5, 전부) 523 / 여유 없음 698 / 여유 없음 +
r<1.2 무시 811. 소품 밑동에 풀이 나는 건 자연스러우니 건물만 피하면 된다.

**1막 벽 콜라이더는 타일 정사각형뿐이라 박힌 바위가 통과됐다.** `wallPatterns`의 벽 타일은
`TILE×TILE` 상자 위에 20면체 바위 4개를 얹는데(반경 `TILE*0.34~0.60`, 오프셋 `±TILE*0.4`),
콜라이더는 `halfW=halfD=TILE/2` 하나뿐이었다. 바위는 그 밖으로 **중앙값 1.64, 최대 2.51**
튀어나온다 — 눈에는 바위가 막고 있는데 몸은 통과했다(실측: 바위를 향해 걸어 들어간 98번 중
30번이 바위 속으로 들어감). `addWallRockCols`가 바위마다 작은 AABB를 얹어 98번 중 96번을
막는다(v727).
**여기서 침범 거리를 안 막으면 던전이 통째로 막힌다.** 1칸 복도는 폭이 `TILE`인데 양쪽 벽이
다 튀어나오므로 여유폭 = `TILE - 2·침범 - 2·반지름`이다. 제한 없이 넣으면 통로가 닫힌다.
그래서 방향별로 남은 폭을 보고 정한다 — 옆 칸이 **깊이도 2칸이고 좌우로도 트여 있을 때만**
방 안쪽으로 보고 `TILE*0.42`, 그 외(1칸 복도·문간)는 `TILE*0.12`.
**깊이만 보면 문간을 방으로 오인한다** — 문 옆 벽은 안쪽으로 깊지만 통로 자체는 1칸이다.
실측으로 갈렸다: 깊이만 보는 판정은 seed 5150에서 오크 반경(0.575)에 67칸이 고립되는데,
좌우까지 보면 0칸이다(6시드 × 30층 나머지는 두 판정이 동일).
크기를 줄이지 말고 **중심을 타일 쪽으로 당길 것** — 줄이면 큰 바위가 얇은 판처럼 느껴진다.
검증은 스크린샷이 아니라 flood fill로 한다(반경만큼 부풀린 콜라이더로 격자를 훑고 플레이어
자리에서 채워 나가 도달 못 한 바닥칸을 센다). 1막 30개 층 × 6시드에서 플레이어 0.5와
오크 0.575는 고립 0칸이어야 한다. 이 방식으로 `?wrockmax`는 0.16부터 층을 막는다는 것도 확인했다.
반경 1.0(보스)은 기준이 아니다 — **보스는 `homeRoom`에 묶여 복도를 지나지 않고,
바위 콜라이더가 없던 예전 빌드에서도 이미 막히는 층이 있다.**

**`flatShading: true`가 지오메트리 노멀을 통째로 무시한다.** 이 게임 캐릭터는 전부
`getLowPolyMat`(=`flatShading: true`)이라, 지오메트리 노멀을 아무리 손봐도 색 패스는 면 노멀을
다시 만들어 쓴다. 노멀을 부드럽게 하려면 그 머티리얼의 `flatShading`도 같이 꺼야 한다.
덤으로: r128 `PolyhedronGeometry`는 detail>0이면 이미 구면(부드러운) 노멀을 갖고 있다
(detail 0만 면 노멀). 즉 몬스터 머리(20면체 detail 1)는 `flatShading`만 꺼도 부드러워진다.

**`BufferGeometryUtils`는 동봉돼 있지 않다**(`mergeVertices` grep 0건). 정점 병합·노멀 평균이
필요하면 위치를 양자화해 직접 묶어야 한다(`_smoothSkinGeo` 참고).

**`cullSceneLights`의 기준점은 카메라가 아니라 플레이어다.** 카메라는 플레이어에서 25칸쯤
떨어져 있어서, '카메라와 플레이어 사이에 있는 벽 횃불'이 내 횃불보다 가깝다고 판정된다.
그래서 걷는 동안 플레이어 광원이 깜빡깜빡 꺼졌다(실측: 3~85층에서 걷는 시간의 5~15%).
플레이어 광원(`torch`/`nearLight`)은 아예 경쟁에서 빼고 슬롯을 예약할 것.

**컬링 판정은 `_culled` 플래그가 아니라 `visible`로 할 것.** 컬링이 끈 광원을 다른 코드가
다시 켜면 (`_culled=true` & `visible=true`) 상태가 되고, `!_culled`로만 보면 그 광원을 건너뛰어
계속 켜진 채 남는다 — 동시 점등 수가 한도를 넘는다(실측 3층에서 6개여야 할 게 7개).
이 수가 흔들리면 three가 머티리얼을 전부 재컴파일한다(아래 항목 참고).

**점광원 상한을 새로 만들지 말 것.** `cullSceneLights(camera)`/`MAX_DYN_LIGHTS`(6)가 이미 있고,
코드 주석 그대로 "렌더 실패(흰 화면)를 막기 위해" 존재한다. 이걸 모르고 별도 상한(`?plcap`)을
넣었다가 두 시스템이 매 프레임 서로의 `visible`을 덮어써서 켜진 광원 수가 6 고정 → 9~16으로
흔들렸고, 개수가 바뀔 때마다 three가 머티리얼을 전부 재컴파일해 셰이더 프로그램이 51→65개로
불어났다. 실제 기기에서 2막 진입 시 컨텍스트 손실 + 흰 화면으로 터졌다(v702에서 제거).

**블룸 임계는 막별로 다르다.** `bloomMat.uniforms.uThresh`는 유니폼 하나이고 각 막 분기가
따로 대입한다 — 1막·3막 0.62, **2막만 0.70**. 2막에만 달빛(방향광)이 있어 금속 갑옷 하이라이트가
임계를 넘어 캐릭터만 유독 번지기 때문이다. 전역으로 올리면 안 된다: 이 게임의 블룸은 임계 바로
위 좁은 구간에 몰려 있어서, 0.66이면 1막 블룸은 4.86%→0.04%로 사라지는데 2막은 1.09%가 남는다
(1막이 먼저 죽는다).

## CrazyGames 준수사항 — 되돌리지 말 것

- 광고 중 게임 오디오는 반드시 음소거(`SFX.suspendForAd()` / `resumeAfterAd()`)
- 일반 접속에서 치트 버튼(`bossTestBtn`/`maxLevelBtn`) 노출 금지
- 미드게임 광고는 자체 하한 2분

## 켜지 않기로 한 것

`?toon=N`(조명 계단화)은 두 번 구현하고 두 번 껐다. 선형 밝기로 자르면 아무 일도 안 일어나고
(확산광이 0~0.2에 몰려 있어 전부 한 칸에 떨어진다), 감마 공간으로 고쳐도 띠가 생기는 대신
화면 전체 노출만 오르내린다. 조명이 너무 균일해서 그렇다 — 켜려면 조명 설계부터 다시 짜야 한다.
코드에 이유가 주석으로 남아 있으니 지우지 말 것.

**골렘 시드에 `Date.now()`를 다시 넣지 말 것.** "개체마다 다른 모양"은 좌표가 이미 보장한다
(골렘은 최소 3칸 간격, 131·977이 서로소라 40칸 맵 안에서 시드가 겹칠 수 없다). `Date.now()`는
거기에 기여하지 않으면서 골렘이 나오는 45층 이상의 렌더 A/B 비교만 불가능하게 만들었다.
지금은 층번호를 섞어 층마다 다르면서도 재현 가능하다.

## 잡동사니

- 임시 디버그 스크립트를 저장소에 만들지 말 것(과거에 `dbg2.js` 등이 섞여 들어간 적 있다)
- 사용자는 주로 한국어로 소통한다. 코드 주석도 한국어다
- 사용자는 모바일에서 작업하는 경우가 많다 — 파일을 주고받기보다 저장소에 올리는 쪽이 다루기 쉽다
  (작업 브랜치에 올려 검증 결과를 보고하고, 머지 지시를 받으면 `main`에 반영 — 위 릴리스 절차 참고)
