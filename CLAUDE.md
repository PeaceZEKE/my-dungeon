# Dungeon Legion — 저장소 안내

Three.js r128으로 만든 아이소메트릭 던전 크롤러 RPG. **`index.html` 단일 파일**(약 1.8MB,
18,000줄 남짓)이 게임 전부다. GitHub Pages가 `main`에서 배포하므로 `main`에 올라가는 순간
라이브다. 현재 CrazyGames 제출을 준비 중이다.

## 파일 구조 — 먼저 알아야 할 것

`index.html` 한 파일에 전부 들어 있고, 앞부분은 **동봉된 라이브러리(미니파이)** 다.
전체를 grep하면 three.js 내부가 쏟아진다. 게임 코드만 보려면 4076줄 이후로 한정할 것.

| 줄 | 내용 |
|---|---|
| ~411 | 스타일·UI 마크업 |
| 434 | three.js r128 (미니파이 동봉 — CDN 의존 제거, 건드리지 말 것) |
| 444 | GLTFLoader r128 (동봉) |
| **4076~** | **게임 코드 시작** |
| 4143 | 플래그 정의 블록 (`AD_TEST`/`DEV_MODE`/`PX_MODE`/`GAME_VERSION`) |

```bash
# 게임 코드만 검색하는 방법
awk 'NR>4076' index.html | grep -n "찾을것"
```

## 릴리스 절차

`GAME_VERSION`(4155줄 부근)을 올리는 것이 릴리스다. 좌측 상단 배지에 표시되고,
사용자가 배포 확인에 쓴다. **코드를 바꿨으면 반드시 같이 올린다.**

```js
const GAME_VERSION = 'v685';
```

푸시 후 확인: `git show origin/main:index.html | grep -o "GAME_VERSION = 'v[0-9]*'"`

**작업은 작업 브랜치(`claude/…`)에서 한다. 검증 결과를 보고한 뒤, 사용자가 머지하라고 할 때만
`main`에 머지한다 — 문서 변경도 예외 없다. 임의로 `main`에 올리지 말 것**(사용자 지시).
`main`에 올라가는 순간 라이브이므로, 머지 전에 최소한 게임 스크립트 문법은 통과시킬 것 —
마지막 `<script>` 블록만 잘라 `node --check`:

```bash
S=$(grep -n '^<script>$' index.html | tail -1 | cut -d: -f1)
E=$(grep -n '^</script>$' index.html | tail -1 | cut -d: -f1)
sed -n "$((S+1)),$((E-1))p" index.html > /tmp/gc.js && node --check /tmp/gc.js
```

문서만 고쳤을 때는 `GAME_VERSION`을 올리지 않는다(배지는 게임 코드 배포 확인용).

**게임 파일을 사용자에게 보낼 때는 항상 두 벌로 보낸다**(사용자 지시) — 내용은 같고 이름만 다르다.

| 파일명 | 용도 |
|---|---|
| `index.html` | 그대로 갖다 쓰는 배포용 |
| `index-vNNN.html` | 버전이 이름에 보이는 보관용 |

새 기능은 URL 플래그로 끌 수 있게 만들되, 보내는 파일에는 **켜진 상태로** 넣는다.
사용자가 모바일에서 URL 파라미터를 붙이지 않고 바로 확인할 수 있어야 한다.

## URL 플래그

| 플래그 | 기본 | 용도 |
|---|---|---|
| `?px=0` | px 켜짐 | 픽셀아트 렌더 모드 끄기(예전 룩) |
| `?dev=1` | 꺼짐 | 보스/만렙 테스트 버튼 노출, 셰이더 에러 로그 |
| `?adtest=1` | 꺼짐 | 광고 테스트(dev 포함) |
| `?blob=0` | 블롭 켜짐 | 발밑 블롭 그림자 끄기 |
| `?blobsteps=N` | 3 | 블롭 그림자 계단 수(0=부드러운 그라디언트) |
| `?toon=N` | 0 | 조명 계단화. **기본 꺼짐 — 아래 참고** |
| `?dspec=N` | 2.0 | 깊이 기반 스펙큘러(가까이 강조·멀리 감쇠). 0=끔 |
| `?dlight=N` | 0.6 | 깊이 기반 직접광 감쇠(멀수록 어둡게). 0=끔 |
| `?dnear=` / `?dfar=` | 20 / 33 | 위 두 효과의 깊이 램프 구간(월드 단위, 실측 기반) |
| `?drough=N` | 1.2 | 러프니스 기반 하이라이트(매끈=강조·거침=감쇠). 0=끔 |
| `?droughlo=` / `?droughhi=` | 0.40 / 0.95 | 위 효과의 러프니스 램프 구간(하단 0.40 이유는 아래 참고) |
| `?spec=N` | 0.85 | 하이라이트 전체 밝기 배수(대비는 유지). 1=보정 없음 |
| `?srough=N` | 1.0 | 바위·돌 재질 러프니스(벽·바닥·기둥·잔해·자갈). 0.9=예전 값 |
| `?organic=0` | 켜짐 | 피부(사람·몬스터) 하이라이트·모서리 강조 제거를 끔 |
| `?osmooth=N` | 80 | 피부 노멀 스무딩 각도 한계(도). 0=끔, 180=모서리까지 전부 뭉갬 |
| `?pgrass=0` | 픽셀 잔디 | 마을·던전 잔디를 예전 3D 풀잎 메시로 되돌림 |
| `?pgfull=0` | 풀 빌보드 | 잔디를 Y축 빌보드(월드 수직)로. 스프라이트가 세로로 눌려 보인다 |
| `?ptrees=0` | 픽셀 나무 | 마을 나무를 예전 20면체 덩어리 나무로 되돌림 |
| `?vout=1` | 외곽선 없음 | 식생(잔디·나뭇잎)에도 검은 외곽선을 다시 그린다 |
| `?pxh=N` | 320 | 픽셀아트 모드의 내부 세로 해상도. **작을수록 픽셀이 커진다**(720 기준 360=배율 2, 320=2.25, 288=2.5, 240=3) |
| `?ssao=N` | 5.0 | 화면공간 AO 세기. 0=끔. `?px=0`에서는 노멀 버퍼가 없어 자동으로 꺼짐 |
| `?ssaor=N` | 2.4 | 위 AO가 가리기를 보는 반경(월드 단위, TILE=5) |
| `?ssaoarch=0` | 건축물만 | AO를 건물·벽뿐 아니라 모든 물체가 만들게 되돌림 |
| `?ashadow=1` | 꺼짐 | 궁수가 진짜 그림자를 다시 지게 함(v741 이전 동작) |
| `?cor=0` | 새 3막 | 3막을 예전 톤(칙칙한 카키 + 자줏빛)·작은 종양으로 되돌림 |
| `?crough=N` | 0.9 | 3막 산호 러프니스. 낮출수록 젖은 것처럼 반짝인다(0.34가 v745 값) |
| `?corbump=N` | 1.0 | 3막 바닥·벽 울퉁불퉁함 배수. 0=평평(예전) |
| `?wpool=0` | 불규칙 | 1막 물웅덩이를 예전 정12각형 원판 + 원둘레 돌로 되돌림 |
| `?mkeep=N` | 1.8 | 원거리 캐스터(마법사류) 유지 거리(TILE 배수) |
| `?mrange=N` | 2.4 | 같은 캐스터의 발사 사거리(TILE 배수). 유지 거리보다 넉넉해야 한다 |
| `?wrock=0` | 켜짐 | 1막 벽 바위 정리(복도 쪽 밀어넣기 + 충돌체) 끄기 — 예전처럼 통과 |
| `?wrockmax=N` | 0.12 | 바위가 1칸 복도·문간으로 튀어나올 수 있는 최대 거리(TILE 배수) |
| `?wrockroom=N` | 0.42 | 같은 값의 방 안쪽 버전(트인 쪽은 바위를 그대로 둔다) |
| `?b3=0` | 새 패턴 | 3막 보스(포란충·탐식 구울·부패한 왕) 행동 선택을 v756의 거리순 if 사슬로 되돌림 |
| `?m3=0` | 3막 몹 켜짐 | 61층 이후에도 2막 몬스터(구울·유령기사·골렘·혼령·궁중마법사)와 복도 함정 골렘이 나오게 되돌림 |
| `?a3lit=N` | 0.62 | 3막 밝기. 석재 반사율 배수이자 **1이면 배경·안개·광원까지 v752 값으로 통째 복원** |

조명 보정을 통째로 끄려면 `?dspec=0&dlight=0&drough=0&spec=1` (전부 중립이어야 청크를 안 건드린다).

일반 접속에서 개발자 버튼은 숨겨진다(CrazyGames 요구사항). 제목을 3초 안에 5번 탭하면 나온다.

## 렌더 변경을 비교할 때 — 이거 안 지키면 틀린 결론이 나온다

스크린샷 A/B를 찍기 전에 **rAF 루프를 끊고, 설정을 바꾸지 않은 대조군 한 쌍을 먼저 찍는다.**
대조군 두 장이 픽셀 단위로 동일할 때만 그 다음 측정을 믿는다.

```js
window.requestAnimationFrame = () => 0;   // 루프 정지
renderWithPost(scene);                    // 이후 렌더는 직접 호출
```

실제로 이걸 안 해서 "화면의 11%가 12/255 이상 변한다"는 결과가 나온 적이 있는데,
전부 반짝이 파티클이 움직인 것이었다. 제대로 재니 최대 9/255였다.
`Math.random`도 시드 고정할 것(장면 생성이 난수에 의존한다) — **단, 아래 시점 규칙을 지켜야 한다.**

테스트는 Playwright + 동봉 Chromium:
`chromium.launch({ args: ['--enable-unsafe-swiftshader','--use-gl=swiftshader','--no-sandbox'] })`

npm의 playwright 버전과 동봉 브라우저 빌드 번호가 어긋나면 `Executable doesn't exist …`로 죽는다.
이때 `playwright install`을 받지 말고 `executablePath`로 동봉본을 직접 가리킬 것
(경로는 환경마다 다르다 — `ls /opt/pw-browsers` 로 확인).

**테스트용 웹서버는 반드시 저장소 루트에서 띄울 것.** 스크래치패드에서 `python3 -m http.server`를
띄웠더니 거기 굴러다니던 **예전 `index.html`이 서빙돼서**, 방금 넣은 함수가 없다며
(`… is not a function`) 테스트가 죽었다 — 코드는 멀쩡했는데 한참을 헤맸다.
띄운 뒤 **버전부터 확인**하면 바로 갈린다:

```bash
curl -s localhost:8899/index.html | grep -o "GAME_VERSION = 'v[0-9]*'" | head -1
```

### 시드는 `addInitScript`로 — 부팅보다 먼저 걸어야 한다

`page.evaluate` 안에서 `Math.random`을 갈아끼우면 **이미 늦다.** 그 시점엔 마을과 부팅 던전이
진짜 난수로 만들어진 뒤고, 그때 생긴 상태가 페이지가 사는 동안 남아 이후 생성 결과를 바꾼다.

```js
await page.addInitScript(() => {
  let s = 12345 >>> 0;
  Math.random = () => { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; };
});
await page.goto(url);   // 이 뒤로는 부팅까지 전부 결정적
```

측정으로 확인한 것:

- `evaluate` 안에서 시드 → 같은 페이지 안에서는 결정적인데 **페이지를 새로 열면 달라진다.**
  대조군끼리도 33층 방 4↔3, 60층 4↔5로 흔들렸다. 이걸 회귀로 오판할 뻔했다.
- `addInitScript`로 시드 → **서로 다른 페이지 로드 두 번이 바이트 단위로 동일.**
  방 개수는 물론 난수 소비 횟수까지 일치한다.
- 부팅 시드만 바꾸고 측정 시드는 고정하면: 던전 격자 해시는 그대로인데 `buildDungeonMesh`의
  난수 소비가 12831↔14357로 바뀐다. 부팅 상태가 이후 생성에 영향을 준다는 직접 증거다.

**THREE 객체를 만들면 그 자체로 난수를 먹는다.** r128의 `generateUUID`가 호출당 `Math.random()`을
4번 쓰고, 모든 `Object3D`·머티리얼·지오메트리 생성자가 이걸 부른다. 그래서 씬에 만드는 객체 수가
달라지면 — 코드 한 줄로 라이트 6개를 덜 만드는 정도만으로도 — 이후 월드 생성 결과가 통째로
바뀐다(실측: 55층 격자 해시 `c0decf18`→`dee17bb8`). 위 "부팅 상태가 이후 생성에 영향을 준다"는
현상의 정체도 이것이다. **객체 개수가 달라지는 변경은 빌드 간 픽셀 A/B가 원천적으로 불가능하므로,
스크린샷 비교 대신 기능을 직접 계측해서 검증할 것.**

**시드가 안 먹는 것처럼 보이면 거는 시점이 늦은 것이다.** `Math.random`을 캡처해 두는 코드는
없다 — 골렘 빌더가 잠깐 바꿔치기했다 되돌리는 곳 한 군데뿐이고, 호출 시점에 잡아서 복원하므로
시드를 지우지 않는다. 월드 생성에 실시간(`Date.now`)이 끼어드는 곳도 이제 없다
(골렘 시드에 있던 `Date.now()`는 v689에서 뺐다 — 아래 참고).

## 반복해서 발목을 잡은 함정들

**TDZ — 두 번 부팅을 죽였다.** 모듈 최상위 코드는 즉시 실행된다. `const`를 그 코드보다
아래에 선언하면 "Cannot access before initialization"으로 게임이 아예 안 뜬다.
플래그·상수는 **쓰는 곳보다 위에** 선언할 것.

**three.js 프로그램 캐시.** `THREE.ShaderChunk.*`를 바꾸고 `needsUpdate`를 줘도 캐시 키에
청크 내용이 안 들어가서 예전 프로그램이 그대로 돌아온다. 런타임 변경 불가 — 컴파일 시점에
굽고 URL 파라미터로 고른 뒤 새로고침해야 한다.

**인스턴스 `onBeforeCompile`이 이긴다.** 이 게임은 풀 바람 등에 자체 `onBeforeCompile`을 쓰는
머티리얼이 많다. 프로토타입에 심으면 770개 중 275개만 걸린다. 공용 ShaderChunk를 고칠 것.

**r128 `WebGLRenderTarget.setSize()`는 `depthTexture`를 안 줄인다.** 리사이즈 핸들러에서
`rt.depthTexture.image.width/height`를 손으로 맞춰야 한다(안 하면 리사이즈 후 외곽선이 깨진다).

**구름 그림자는 평면이 아니라 포스트에서 깊이로 건다.** 예전에는 y=0.14짜리 어두운 평면 한 장이라
그보다 높은 것(건물·나무·캐릭터)은 깊이 검사에서 평면보다 앞이라 그림자가 아예 안 걸렸다
(실측: 바닥 63.8% / 오브젝트 0.5%). 깊이를 `uInvVP`로 월드 좌표로 되돌려 XZ로 구름을 읽으면
전부 걸린다(오브젝트 53.8% / 바닥 23.7% — 같은 구름 무늬가 화면을 가로지른다).
역행렬은 **픽셀 스냅을 풀기 전에** 갱신할 것 — 색·깊이를 찍은 그 카메라여야 한다.
던전·쉼터에서는 `uCloudStr`을 0으로 둬서 셰이더가 통째로 건너뛴다.

**같은 렌더 타깃에 두 번 그리려면 `scene.background`를 꺼야 한다.** `renderer.autoClear = false`로
막았다고 생각해도, `scene.background`가 Color면 three의 `WebGLBackground.render`가 `forceClear`를
켜서 무조건 clear한다. 노멀 패스를 2패스로 나눴다가 2패스가 1패스 결과를 통째로 지워서
모서리 하이라이트가 화면 전체에서 사라졌다(캐릭터를 다 숨겨도 사라져서 원인 찾기가 오래 걸렸다).
2패스 동안 `background = null`로 두고 끝나면 되돌릴 것.

**피부는 노멀 버퍼에서 '빼는' 게 아니라 '표식으로 덮는' 것이다.** 그냥 빼면 그 자리에 뒤에 있는
벽·바닥의 노멀이 드러나고, 그 경계선이 피부 위에 하이라이트로 그려진다(실측: 피부 픽셀의 10.9%,
최대 49/255). 순수 빨강(1,0,0)을 찍어 두고 셰이더에서 걸러야 0%가 된다 — 인코딩된 노멀은
g·b가 -0.7 아래면 r이 ±0.14를 못 넘으므로 이 조합이 정상 노멀과 겹치지 않는다.
표식 대상은 레이어 1로 고른다(게임 코드는 레이어 0만 쓴다). 드로우콜은 늘지 않는다 —
1패스에서 빠진 피부가 2패스로 옮겨갈 뿐이다(실측 5671 → 5671).
레이어는 `_hideNonSolid`가 매 프레임 붙인다(캐릭터 만들 때가 아니라). 1패스에서 빼는 판정과
2패스에서 덮는 판정이 같은 줄에서 나와야 둘이 어긋나지 않기 때문 — 나중에 추가된 메쉬가
빠지면 그 자리에만 구멍이 남아 원인 찾기가 고약하다.

**새 몬스터의 피부: `markOrganic`만 부르면 나머지는 자동이다.** 하이라이트 제거·노멀 스무딩·
노멀 패스 표식이 전부 `userData._organic`/`_skin` 표식을 보고 돌아간다.
`createAdvancedMonster`의 타입 분기에서 만든 `skinMat`은 이미 자동으로 걸린다
(`type !== 'darkknight' && type !== 'golem'`) — **새 타입이 돌·금속이면 이 제외 목록에 넣을 것.**
자동이 아닌 경우는 하나다: 전용 빌더(`buildXxxBody`)가 바깥 `skinMat`을 가리고 자기 재질을
따로 만들 때. 실제로 궁중 마법사·혼령·시종장의 망령이 이렇게 빠져 있었다(v706에서 수정).
전용 빌더를 새로 쓰면 살·뼈 재질에 `markOrganic`을 직접 걸어야 한다.

**픽셀 잔디(pixel-grass)는 조명을 안 받는다.** 인스턴싱 빌보드 + 자체 ShaderMaterial이라
라이트도 톤매핑도 자동으로 안 걸린다. 그대로 넣었더니 캄캄한 동굴에서 잔디만 발광했다.
그래서 두 가지를 붙여 뒀다 — 지우지 말 것.
· 프래그먼트에서 ACES 톤매핑을 직접 계산한다(`renderer.toneMappingExposure`와 같은 값).
  안 하면 잔디만 밝게 떠서 배경에 붙인 스티커처럼 보인다.
· `litNear`/`litMin`으로 플레이어 횃불 기준 거리 제곱 감쇠. 선형(smoothstep)으로 하면
  중간 거리가 너무 밝게 남는다 — 점광원(decay=2)과 같은 모양이어야 자연스럽다.
마을은 넓게 조명되므로 감쇠를 끄고(`litNear:0`) 낮/밤만 반영한다.

**Y축 빌보드는 이 카메라에서 스프라이트를 절반으로 눌러 버린다.** iso 카메라가 수평에서 51.9°
내려다보므로(오프셋 11.07/20/11.07), 월드 수직 1은 화면에서 0.475로 줄어든다. 원본 모듈의 Y축
빌보드(세로축=월드 +Y)를 그대로 쓰면 잎 그림이 세로로 눌려 뭉개진 얼룩처럼 보인다.
가로·세로를 모두 카메라 축에 맞추는 풀 빌보드가 기본이다(`?pgfull=0`으로 예전 방식 비교).
눌림(밟힘)은 `world.y *= ...`가 아니라 잎 축(up) 방향으로 빼야 한다 — 안 그러면 풀 빌보드에서
잎이 짧아지는 대신 기운다.

**잔디는 노멀 패스에서 반드시 빼야 한다**(`userData.fx` → `_hideNonSolid`). `overrideMaterial`이
프래그먼트 `discard`를 없애 버려서, 알파로 모양을 낸 잎이 전부 불투명 사각형으로 찍힌다.

**검은 외곽선 스위치는 노멀 버퍼의 알파다.** 외곽선은 깊이 차이로 그리므로 노멀 표식색(RGB)으로는
못 끈다. 알파를 따로 쓰면 RGB(=진짜 노멀)를 남길 수 있어서 **나뭇잎은 모서리 하이라이트를 유지한 채
검은 선만** 뺄 수 있다. 잔디는 노멀이 의미 없으니 초록 표식(0,1,0)까지 얹어 하이라이트도 뺀다.
식생 패스(3패스)는 `overrideMaterial`을 쓰지 않고 메시마다 자기 표식 재질로 바꿔 그린다 —
잔디는 알파 `discard`가 살아 있어야 잎 모양대로 찍히고 잎은 진짜 노멀이 필요해서, 요구가 서로
다르기 때문이다. 대상이 메시 두어 개뿐이라 재질 교체가 오히려 싸다(실측 추가 비용: 드로우콜 ≤2,
삼각형 +4,458 = 화면 안 잔디 한 번 더).

**덩어리는 잎보다 어두워야 한다.** 같은 밝기로 두면 덩어리가 캐노피처럼 보이고 그 위의 작은 잎이
묻힌다(`canopyBodyDim` 0.72). 밝기는 `aTint`에 곱해 넣는다 — 색 양자화 뒤에 곱해지는 값이라
밴드를 건드리지 않고 한 단 눌러 준다. 덩어리 구는 20면체(detail 0, 20삼각형)보다 저해상도 구가
삼각형당 둥글기가 좋다(6×5 = 48삼각형 / 20면체 detail 1 = 80삼각형).

**잎 사이 빈틈은 카드 수로 메우지 말 것.** 작은 잎으로 캐노피 표면을 다 덮으려면 그루당 카드가
150장 넘게 필요해 삼각형이 몇 배로 뛴다. 가이드 구체를 저폴리 덩어리(`canopyBody`)로 깔면
잎 사이로 지면 대신 캐노피 안쪽이 보인다 — 그루당 삼각형 80개면 된다.
덩어리를 넣으면 **잎의 `castShadow`는 끄고 덩어리만 그림자를 지게 한다**(그림자 패스 하나가
잎 삼각형만큼 통째로 드는데, 실루엣은 거의 같다). 덩어리 인스턴스에는 회전을 주면 안 된다 —
잎 배치는 회전하지 않은 blob 좌표를 쓰므로 어긋난다. jitter는 인스턴스 이동에 넣으면 정확히 맞는다.

**카드 한 장에 잎 여러 장을 겹쳐 굽는다**(`leafBlades`). 카드=잎 한 장이면 캐노피를 채우는 데
카드가 너무 많이 필요하다. 겹친 잎끼리는 ① `aSprig`로 밝기를 어긋내야 한 장처럼 붙지 않고
② z를 조금씩 띄워야 같은 평면에서 깊이 싸움으로 지글거리지 않는다.
잎 다섯 장짜리 큰 카드는 고사리처럼 보이고 삼각형도 168k까지 오른다 — 세 장짜리 작은 카드를
많이 뿌리는 쪽이 낫다(잎당 삼각형 = `leafBlades × leafSeg × 5/3`).

**가늘고 뾰족한 잎(`leafShape:'blade'`)은 방향을 직접 잡아 줘야 한다.** 무작위 Z회전을 그대로 두면
바늘이 사방으로 흩어져 형태가 안 잡힌다. 구체 바깥 방향을 잎 평면에 투영해(`lookAt` 직후 행렬의
열이 잎의 로컬 축) 그쪽으로 돌린다. 명암도 월드 Y가 아니라 잎 길이 방향(`uv.y`)으로 줘야
밑동이 어둡고 끝이 밝은 잔디 명암이 된다. 잎이 길면 캐노피가 야자수처럼 되므로 짧게·촘촘하게.

**잎 모양이 안 보이면 지오메트리가 원이기 때문이다.** 셰이딩만 손봐서는 '테두리 있는 원'까지가
한계다. `ptLeafGeo`가 물결진 다각형(r = 1 + amp·cos(lobes·θ))을 굽는다. 순수 cos이면 봉우리가
뾰족해 별 모양이 되므로 지수 0.55로 봉우리를 넓히고 골을 얕게 한다.
테두리 판정은 반지름이 아니라 `uv.x`(중심 0 → 테두리 1)로 해야 모양이 원이 아니어도 맞는다.
잎당 삼각형이 `leafSides`개라 개수·크기로 균형을 잡을 것(잎당 24분할·32장이면 마을 삼각형이
132k → 181k까지 오른다. 20분할·28장이면 157k).

**캐노피가 '구 여러 개 붙은 덩어리'로 보이면 잎 단위 셰이딩이 없는 것이다.** 명암을 가이드 구체의
바깥 방향으로만 내면 같은 명암대에 있는 이웃 잎끼리 색이 완전히 같아서 한 덩어리로 뭉친다.
잎 로컬 좌표(`vLocal`)로 ① 위는 밝고 아래는 어두운 기울기를 명암대 나누기 '전에' 더하고
② 테두리를 한 단 어둡게 찍으면 잎마다 윤곽이 보인다. 테두리 반지름(`leafRimAt`)을 너무 낮추면
테두리가 두꺼워져 잎이 동전처럼 보인다(0.72는 두껍고 0.84가 적당).
이때 잎의 무작위 Z회전은 빼야 한다 — 로컬 +y가 제각각이면 ①의 방향이 무의미해진다.

**ACES는 밝은 쪽 채도를 눌러서 선명한 색을 못 만든다.** 픽셀아트 레퍼런스의 쨍한 초록(#94ce50)을
목표로 입력색을 역산해 봤더니 어떤 값을 넣어도 #bfce99 같은 뿌연 연두로만 나온다(`aces.js` 역산).
그래서 나뭇잎은 톤매핑을 끄고(`useACES:false`) 적은 색이 그대로 나오게 한다 — 대신 색을 씬 밝기에
맞춰 직접 골라야 한다. 잔디는 지금 톤이 맞으므로 ACES를 켠 채로 둔다.

**픽셀 나무는 잔디와 반대로 노멀 패스에 넣어도 된다.** 잎이 알파가 아니라 육각 지오메트리라
`overrideMaterial`이 `discard`를 지워도 실루엣이 같다. 대신 바람은 반드시 `instanceMatrix`를
CPU에서 갱신할 것 — 정점 셰이더로 흔들면 색 패스만 흔들리고 노멀·그림자 패스는 제자리라
외곽선이 형상과 어긋난다.

**인스턴싱 나무를 마을 씬에 그냥 넣으면 마을 전체가 막힌다.** 마을은 `townScene.children`을
훑어 바운딩 박스로 콜라이더를 자동 등록하는데, 인스턴싱 그룹은 바운딩이 숲 전체다.
`group.userData.noCollide = true`로 자동 등록에서 빼고 그루마다 `_collide`를 직접 부를 것.
콜라이더는 잔디 배치(`buildTownGrass`)보다 먼저 등록돼야 나무 자리에 풀이 안 난다 — 지금 순서가
자동 등록(6771) → 잔디(6804)라서 맞는다.

**셀 배치에 콜라이더 여유를 주면 밀도가 무너진다.** 마을 링의 콜라이더 283개 중 235개가
가로등·통 같은 소품이다. 점으로 뿌리던 예전과 달리 셀은 1.6칸이라 소품 하나가 그만한 구멍을
낸다. 실측 셀 수: 링 전체 964 / 예전 규칙(+0.5, 전부) 523 / 여유 없음 698 / 여유 없음 +
r<1.2 무시 811. 소품 밑동에 풀이 나는 건 자연스러우니 건물만 피하면 된다.

**1막 벽 콜라이더는 타일 정사각형뿐이라 박힌 바위가 통과됐다.** `wallPatterns`의 벽 타일은
`TILE×TILE` 상자 위에 20면체 바위 4개를 얹는데(반경 `TILE*0.34~0.60`, 오프셋 `±TILE*0.4`),
콜라이더는 `halfW=halfD=TILE/2` 하나뿐이었다. 바위는 그 밖으로 **중앙값 1.64, 최대 2.51**
튀어나온다 — 눈에는 바위가 막고 있는데 몸은 통과했다(실측: 바위를 향해 걸어 들어간 98번 중
30번이 바위 속으로 들어감).
**콜라이더만 얹으면 던전이 통째로 막힌다.** 1칸 복도는 폭이 `TILE`인데 양쪽 벽이 다 튀어나오므로
여유폭 = `TILE - 2·침범 - 2·반지름`이다. 그래서 **콜라이더가 아니라 바위 자체를 벽 안으로
밀어 넣는다**(`fitWallRocks`, v728). 콜라이더는 밀어 넣은 자리에 그대로 얹으므로 보이는 것과
막히는 것이 항상 일치한다 — 98번 중 98번을 막고, 복도 통행폭은 예전과 **완전히 동일**하다
(단면 485개 중앙 5.05·최소 2.30이 양쪽 같음). 콜라이더만 당겼던 v727은 큰 바위 겉면이
어긋나 2번이 통과됐고, 복도가 좁아져 오크 반경에서 막히는 층이 생겼다.
허용치는 방향별로 남은 폭을 보고 정한다 — 옆 칸이 **깊이도 2칸이고 좌우로도 트여 있을 때만**
방 안쪽으로 보고 `TILE*0.42`, 그 외(1칸 복도·문간)는 `TILE*0.12`.
**깊이만 보면 문간을 방으로 오인한다** — 문 옆 벽은 안쪽으로 깊지만 통로 자체는 1칸이다.
실측으로 갈렸다: 깊이만 보는 판정은 seed 5150에서 오크 반경(0.575)에 67칸이 고립되는데,
좌우까지 보면 0칸이다(6시드 × 30층 나머지는 두 판정이 동일).
크기는 줄이지 말 것 — 반경 최대 `TILE*0.6`=3.0이고 좁은 쪽 허용 끝이 3.1이라 **어떤 바위든
밀어 넣기만 하면 들어간다.** 줄이면 큰 바위가 얇은 판처럼 느껴진다.
좁은 쪽에서는 콜라이더(정사각형 `0.72r`)가 벽 상자 안에 거의 다 들어가 등록 자체가 생략된다 —
복도에서는 사실상 예전 그대로 벽 하나로 막고, 바위는 시각적으로만 물러난다. 그래서 `walls`가
덜 늘어난다(25층 245 → 329. v727은 494였다).
검증은 스크린샷이 아니라 flood fill로 한다(반경만큼 부풀린 콜라이더로 격자를 훑고 플레이어
자리에서 채워 나가 도달 못 한 바닥칸을 센다). 1막 30개 층 × 6시드에서 플레이어 0.5·오크
0.575·여유 0.6 모두 **예전 빌드와 완전히 같은 결과**여야 한다. 반경 1.0(보스)은 기준이 아니다 —
**보스는 `homeRoom`에 묶여 복도를 지나지 않고, 바위가 없던 예전 빌드에서도 이미 막히는 층이 있다.**
복도 스크린샷으로 확인하려 들지 말 것 — 플레이어를 순간이동시켜도 카메라가 따라가지 않고,
복도는 캄캄해서 형태가 안 보인다. 통행폭을 직접 재는 쪽이 빠르고 정확하다.

**건물이 깜빡이면 AO보다 먼저 '면이 겹치는 상자'를 의심할 것.** 대장간(-13,0)의 석재 기단이
몸통과 **정확히 같은 크기**(둘 다 5.2×4.4)라 옆면 네 개가 완전히 같은 평면이었다 — 벽 밑동
0.5구간이 회벽↔석재로 프레임마다 뒤집혔다(실측: 그 구간 표본의 23.7%가 프레임 간 12/255 넘게
바뀜. 같은 벽 위쪽은 4.3%). 기단을 0.1씩 넓혀 `5.4×0.5×4.6`으로 하면 3.8%로, 벽 다른 부분보다
낮아진다. **교회 기단은 원래 이 규칙이다**(본당 4.6×7.2 / 기단 4.8×7.4) — 새 건물도 따를 것.
찾는 법은 씬 전체 상자 쌍을 훑어 '평행한 면의 좌표가 1e-3 안에서 같고 나머지 두 축에서 겹치는'
경우를 세는 것이다. 마을 220개 상자에서 대장간이 겹침 면적 2.6·2.2로 압도적 1위였고(2위 0.36)
사용자가 "저 집만 유독"이라고 한 것과 정확히 일치했다.
**이때 측정점은 반드시 '보이는 면' 위에 잡아야 한다.** 기단을 튀어나오게 고친 뒤에도 예전
평면(x=-10.8)에서 재면 밑동이 여전히 11~43% 튀는 것처럼 나온다 — 점이 새 기단 면보다 0.1
안쪽이라 화면에서 1~2픽셀 어긋나 기단 윗모서리를 걸치기 때문이다. 면을 맞춰 주면 3.8%다.
부호를 반대로 잡아 0.1 뒤쪽에서 재면 또 다른 자리에 26~40% 봉우리가 생긴다.

**3막을 원화에 맞출 때 걸린 것들(v742).** 예전 3막은 2막과 톤이 거의 같았다 — 석재가 칙칙한 카키
(`0xa89f82`)에 환경광 0.2, 그림자색이 자주라 2막의 청보라와 명도·채도가 비슷했다. 원화의 대비는
**따뜻한 크림 석재 ↔ 청록 그늘 ↔ 채도 높은 산호빛 종양**이고, 아래를 지켜야 그 그림이 나온다.
· **환경광까지 청록으로 두면 안 된다.** 아이보리 석재가 통째로 회녹색이 된다. 밝은 쪽은 따뜻하게
  (`ambientLight` 0x585a4c) 두고 청록은 포스트의 `uShadowColor`가 그늘에만 얹는다.
· **종양은 그림자를 지면 안 된다.** 그림자 광원이 점광원(큐브 6면)이라 캐스터 하나가 여러 번
  그려진다 — 산호를 캐스터로 두자 삼각형이 **170k → 526k**로 뛰었다.
· **알갱이는 한 지오메트리로 합친다**(`mergeCoral`). `BufferGeometryUtils`가 없으므로 20면체
  비인덱스 정점을 직접 옮겨 붙이고 색은 정점색으로 넣는다. 덩어리당 메시가 1개라 알갱이를
  3배로 늘려도 드로우콜은 오히려 준다(같은 층·같은 방 배치에서 `?cor=0` 4017콜/95k삼각형 →
  새 3막 3418콜/89k삼각형).
· **합친 알갱이에 맵을 씌우지 말 것.** 알갱이마다 구 전체 UV가 들어가서 젖은 살 텍스처가
  체커처럼 반복돼 결정 조각으로 보인다. 정점색 + `flatShading`이면 충분하다.
· **알갱이는 작게·많이.** 크고 적게 두면 20면체 면이 커져 콜리플라워가 아니라 크리스털이 된다.
· **크기 기준은 TILE이다.** 예전 `makeFleshClump(1.2)`는 지름 1 남짓이라 화면에서 얼룩으로만
  보였다. 원화의 종양은 타일(5)의 절반 이상을 덮는다.
· **`TubeGeometry`는 굵기가 일정하다 — 그대로 쓰면 덩굴이 각목이 된다.** 링마다 곡선 위의 점에서
  잰 거리를 줄여 끝으로 갈수록 가늘게 해야 한다(`makeTaperedTube`. 정점 순서는
  `(tubularSegments+1)` 링 × `(radialSegments+1)`). 색이 밝으면(예: `0xd49fae`) 위쪽 모서리에
  하이라이트가 한 줄 앉아 판자로 읽힌다.
· **덩굴은 공중을 가로지르면 안 된다 — 면에 붙여야 덩굴로 보인다.** 벽면을 따라 내려와 밑동에서
  바닥으로 꺾여 널브러지거나(그 반대로 올라가거나), 기둥을 나선으로 감는 형태여야 한다.
  벽 덩굴은 **진짜 벽 타일에 붙여야** 하므로 방 경계에서 벽 칸을 찾아 그 칸과 방 안쪽 법선을
  넘긴다(`isF`로 문간을 걸러낼 것 — 안 그러면 통로 한가운데에 선다).
  붙는 면에서 0.3 띄우지 않으면 벽과 깊이 싸움이 난다.
· **타일 높이를 흔들려면 판을 두껍게 해야 한다.** 두께 0.4에 ±0.11 오프셋 + 기울임을 주면
  이웃 타일 사이로 옆이 비친다. 0.55로 올리면 단차·기울임을 다 합쳐도 안 뚫린다.
  **타일 높이를 흔들면 그 위 장식(복도 살길·핏줄·발광 균열)도 같은 오프셋을 따라가야 한다** —
  안 그러면 낮아진 타일 위에 떠 보인다(`corTileY` 맵에 기억해 둔다).
· **바닥 핏줄을 곧은 원기둥으로 눕히면 성냥개비가 된다.** 예전 `makeFloorVeins`는 길이
  `TILE*(0.4~0.95)`(실측 2.64)에 반지름 0.05인 4각 실린더를 무작위 각도로 흩었다 —
  사용자가 "이 빨간 막대기 뭐냐"고 물은 게 이것이다. 굽이치는 곡선 + `makeTaperedTube`로
  끝을 가늘게 하고 밑동에서 한 번 갈라지게 해야 기어가는 핏줄로 읽힌다.
· **바닥 굴곡은 `displaceFloorTop`으로.** 윗면만 사인파로 밀고 가장자리는 페이드로 평평하게
  남기므로 인접 타일과 틈·단차가 안 생긴다. 분할은 4로는 각져서 6이 필요하다.
  타일마다 y를 ±0.055 흔들고 90° 단위로 돌리면 격자가 자로 잰 듯 보이지 않는다.
· **벽돌을 균등 분할하면 줄눈이 일정해진다.** 폭과 간격을 따로 뽑아 누적으로 쌓고
  높이·돌출·기울기까지 흔들 것(`makeCorruptWallTile`의 COR_V2 분기).
· **산호 숨쉬기(스케일)는 지오메트리를 자기 중심으로 옮겨야 제자리에서 부푼다**(`coralMesh`).
  벽 산호처럼 원점에서 떨어진 덩어리를 그냥 스케일하면 타일 원점 쪽으로 미끄러진다.
  실측: 중심 이동 0.02 미만, 크기는 폭 −4.5% / 높이 +4.4%로 진동.
  기존 `corruptPulses`의 `mesh` 스케일과 겹치므로 COR_V2에서는 그쪽을 `null`로 둔다.
· 빌드가 달라지면 층 장식의 난수 소비가 달라져 **빌드 간 드로우콜 비교는 무의미하다.** 다만
  방 격자는 장식보다 먼저 생성되므로 **같은 빌드 안에서 `?cor=0` 대조는 유효하다**(위 수치).

**3막 몬스터(v750)를 넣으면서 걸린 것들.**
· **뷰어 모델을 옮길 때 팔레트를 '보기 좋게' 고치지 말 것.** `viewers/` 의 색은 게임 조명으로
  역산한 값이라 화면에서 맞다 — 뷰어 안에서 밝아 보이지 않는 것이 정상이다. 부패한 왕이 한 번
  예외였는데(v13은 예전 렌더 경로라 창백한 색이 3막에서 흰죽처럼 떴다 — 재질 16종이 전부
  출력 휘도 119~157에 몰리고 평균 채도 0.093), v14에서 뷰어 자체가 게임 경로로 옮겨지며 해결됐다.
  **다만 v14의 3막 프리셋은 v752 환경이라 v753(어두워진 3막)에서는 뷰어보다 어둡게 나온다** —
  실측: 같은 모델이 v752 조명에서 평균 휘도 84·채도 0.198, v753 조명에서 65·0.157.
· **색만 다른 재질은 `userData.mergeInto`로 묶어 정점색으로 굽는다**(`c3MergeStatic`이 처리 —
  3막 산호 `mergeCoral`과 같은 방식). 드로우콜은 (병합 그룹 × 재질)이라 재질 수가 그대로 콜 수다.
  부패한 왕은 이걸로 **개체당 메시 139 → 46**(삼각형은 20,802 → 20,202으로 잔해 제거분만 줄었다).
  묶으려면 색 말고 나머지가 같아야 한다 — 살(markOrganic·러프니스 1.0) / 금속(0.6·0.3) /
  단단(0.9·0) 세 무리. 발광 재질은 emissive 색·세기가 제각각이라 못 묶는다.
  **매 프레임 모양이 바뀌는 것은 병합에서 빼고 CPU로 정점을 갱신할 것** — 왕의 발광 핵 11개가
  그렇다(그룹째 스케일은 원점 쪽으로 미끄러지고, 정점 셰이더는 노멀·그림자 패스가 제자리라
  외곽선이 어긋난다. 둘 다 CLAUDE.md에 이미 적힌 함정이다).
· **몬스터 하나를 던전에서 눈으로 확인하려면 층 장식을 숨겨야 한다.** 3막 바닥·벽에 산호 종양이
  많아서 개체가 그 뒤에 묻힌다. `bb.max.y > 0.25`인 메시를 임시로 `visible=false` 하고 찍을 것.
  카메라를 개체 쪽으로 당길 때 플레이어도 같이 숨기지 않으면 화면 절반이 기사다.
· **공용 애니메이션이 `limbs.legR/legL/armR/armL`의 rotation을 무조건 건드린다.** 곤충처럼 팔이
  없는 리그는 더미 그룹을 물려 두지 않으면 보스 돌진·근접 윈드업 분기에서 죽는다(`c3BugLimbs`).
· **날개를 실제로 퍼덕이면 안 된다.** 초당 수십 회로 회전시키면 60fps에서 앨리어싱이 나 툭툭
  끊긴다. 각도만 어긋낸 반투명 날개 두세 장을 겹쳐 '흐릿한 잔상'을 굽고 그룹만 천천히 흔들 것.
· **곤충 보행은 삼각보행(위상 π 두 조)이어야 한다.** 위상을 조금씩 어긋내는 파도보행으로 하면
  여섯 다리가 물결쳐 벌레가 아니라 지네로 읽힌다.
· **비행 몹의 부양 높이는 `baseY`가 아니라 별도 오프셋으로 준다.** `baseY`를 올리면 블롭 그림자가
  같이 떠오른다(`updateBlobShadows`가 `y - baseY`만 빼기 때문). 점프와 같은 취급으로 두면
  그림자가 바닥에 남는다.
· **뷰어 최상위의 `rotation.y = Math.PI`는 옮기지 말 것.** 게임 규약(-z 정면)이 정해지기 전
  뷰어들이 정면을 +z로 돌려 놓은 잔재다. 지오메트리 자체는 이미 -z를 본다(부패한 왕: 코 z=-0.28).
  넣으면 게임에서 뒤통수를 보고 걷는다.
· **뷰어에서 옮긴 보스는 스케일 1.0인데 반지름은 0.5로 잡힌다.** 몸집이 실물 크기(높이 3.3~3.9)라
  플레이어가 몸 안으로 걸어 들어간다 — `C3_BOSS_RADIUS`로 따로 곱해 준다.
· **`c3MergeStatic`으로 병합하면 새 몬스터가 예전 몬스터보다 오히려 싸다.** 실측 85층 같은 조건:
  `?m3=0`(2막 몹) 5,225메시·드로우콜 8,696 / 3막 몹 1,574메시·드로우콜 9,041(다만 난수 스트림이
  달라져 층 장식이 달라진 값이라 절대 비교는 아니다. 개체당: 포자 벌레 24메시·2,602삼각형,
  농침벌 15·1,281, 부패한 구울 30·4,350, 살덩이 거인 37·9,382).
· **느린 거인은 방당 수를 묶어야 한다.** 골렘과 같은 이유다 — 안 묶으면 85층에서 살덩이 거인이
  17마리 나와 전투가 통째로 늘어졌다(방당 1~2로 제한해 8마리).
· **막이 바뀌면 이전 막 전용 스폰 코드를 층번호로만 막지 말 것.** 복도 함정 골렘이 `floor >= 45`
  하나뿐이라 3막에도 계속 나왔다 — 새 막은 새 몬스터라는 규칙을 깼다. `<= 60`을 같이 걸어야 한다.

**보스 행동을 '거리순 if 사슬'로 고르면 가까운 패턴 하나만 나온다(v757).** 3막 보스 셋이 전부
`if (d < 2.4) A; if (d < 10) B;` 꼴이었는데, 보스는 계속 붙으러 오므로 쿨이 도는 시점엔 이미
A의 사거리 안이다 — B는 영원히 안 나온다. 실측(70초 × 유지거리 3종): 포란충 **점액 0회**,
탐식 구울 **삼키기 0회·위액 0회**, 부패한 왕 **방패 210초 중 1회**. 보스마다 패턴이 사실상
하나였다. `c3Pick([[이름, 가중치], …])`로 가중 추첨하고 **가중치만 거리로 흔들면** '거리에 맞는
행동'은 유지되면서 나머지가 섞여 나온다(같은 조건에서 점액 9~19 · 삼키기 4~34 · 방패 7~22회).
**패턴을 늘리면 밀착 피해가 같이 뛴다** — 포란충 점액은 부채꼴 3발인데 붙어 있으면 세 발이
같은 자리에 꽂혀 근접 초당피해가 1012 → 1421로 올랐다. 밀착(2.6칸 이내)에서는 한 발만 뱉게 했다.

**'가끔 터지는 강화 폭발'에 영구 버프를 넣으면 긴 싸움에서 무한히 세진다(v757).** 부패한 왕의
오염 폭발이 터질 때마다 `e.damage *= 1.15; e.speed *= 1.12`가 걸려 있었다. 2페이즈 진입 연출인 줄
알았는데 분노 뒤에도 계속 선택되는 패턴이라 **120초에 14번** 터졌다 — 공격력 675 → 5502,
속도 3.2 → **15.64로 플레이어(6.5)보다 빨라졌다.** 강화는 전환 1회만(`ckRaged` 플래그).

**돌진기는 '실제로 몇 칸을 덮는지' 재 보고 사거리를 정할 것(v757).** 탐식 구울의 삼키기는
`속도 × 9.0 × 0.4초`라 실이동이 **1.7칸**뿐이었는데 선택 조건은 9칸이었다 — 방 한가운데 열린
자리에서 재니 **2·3·4칸 간격 전부 빗나갔다**(이동 1.35 / 4.05 / 6.75, 명중선 3.5).
`속도 × 15 × 0.6초`로 늘리면 이동 24.75~27로 **셋 다 명중**한다. 선택 조건도 덮는 거리(5칸)로 좁혔다.

**보스 몸집을 키우면 벽 근처가 통째로 사각지대가 된다.** 콜라이더 반지름만큼 부풀린 격자로
보스 방을 flood fill 해 보면(4시드 × 3층) 방은 전부 하나로 이어져 있지만, **플레이어가 설 수 있는
바닥 중 보스가 설 수 있는 비율**은 포란충(r=1.3) 93~94% · 부패한 왕(r=1.0) 97% · **탐식 구울
(r=2.4) 85~86%** 다. 벽에서 2.4 안쪽 띠에 붙으면 걸어서는 못 닿는다 — 그래서 이런 보스는
돌진기가 그 띠를 덮어야 한다(위 항목이 그 이유다).

**보스 계측 하네스에서 조준을 `player.yaw`에 직접 대입하면 안 된다.** `update()` 안에서 마우스
지면 교점으로 매 프레임 덮어쓰기 때문에 지워진다 — 모르고 재면 스윙이 **한 대도 안 맞는데**
(실측 총피격 0, 정면 내적 −0.7) 코드는 멀쩡하다. 보스를 화면에 투영해 `mousePos`에 넣을 것:
`const v = e.obj.position.clone(); v.y = 0; v.project(camera); mousePos.x = v.x; mousePos.y = v.y;`
같은 하네스에서 두 가지가 더 걸렸다. ① **플레이어를 좌표 대입으로 움직이면 벽을 통과한다** —
방 밖 암석 속에 선 플레이어를 보스가 못 따라와 '피해 0'이 나온다(축별 `walls` 검사를 넣을 것).
② **빌드가 다르면 난수 스트림이 어긋난다** — `c3Pick`이 `Math.random`을 먹어서 이후 층 생성이
통째로 달라진다. 싸움마다 시드를 다시 걸어야 두 빌드가 같은 방에서 붙는다.

**'유휴 70%'는 죽은 시간이 아니다.** 특수 패턴 상태가 아닌 프레임을 세면 3막 보스가 70% 안팎인데,
그 시간에 기본 근접 공격이 돈다(상태별 피해를 갈라 보면 `none` 구간 피해가 가장 크다).
쿨다운을 더 깎아 이 숫자를 낮추려 들지 말 것 — 특수기만 연달아 나오는 보스가 된다.

**살덩이가 '깨진 결정 조각'처럼 보이면 지터를 의심할 것(v754).** 사용자가 3막 살덩이 몬스터
셋의 "몸에 붙은 포자"를 피부 재질로 바꿔 달라고 했는데, 재질은 이미 `markOrganic`이 걸려
있었다(`_skin`·`_organic`·`flatShading:false`·노멀 스무딩까지 전부 O). 문제는 재질이 아니라
**전역 `jitterGeometry`가 정점을 독립적으로 흔든다**는 것이었다.
`IcosahedronGeometry(r, 0)`은 비인덱스라 **정점 60개 = 고유 좌표 12개**인데, 지터 뒤에는
고유 좌표가 **60개**가 된다 — 삼각형 20장이 제각각 떨어져 나가 껍질이 깨진다(실측: 이웃 면
노멀 각도 49.4°). 30% 진폭이면 조각이 눈에 그대로 보인다.
**노멀 스무딩으로는 못 되돌린다** — `_smoothSkinGeo`는 좌표를 양자화해 묶는데 이미 벌어져서
같은 자리로 안 모인다. 뷰어(`viewers/*`)의 `jitterGeometry`와 골렘의 `jitterGeoShared`는
둘 다 **좌표가 같은 정점에 같은 오프셋**을 주는 판이다 — 살덩이를 옮길 때 이쪽을 써야 한다
(`c3JitterShared`). 살덩이 혹처럼 **비인덱스 다면체**에만 생기는 문제다(원기둥·상자는 인덱스라
정점이 공유돼 안 갈라진다).

**"3막이 너무 밝다"의 범인은 조명이 아니라 석재 반사율이었다(v753).** 사용자가 "벽 너머까지
다 보인다"고 했다. 적을 지우고 같은 조건에서 화면을 위아래 4구간으로 나눠 평균 휘도를 재면
**1막 [25,39,35,25] · 2막 [27,44,41,31] · 3막 [64,101,92,61]** — 3막만 두 배다.
**끄면서 갈라 보는 것이 먼저다.** 환경광·달빛·반구광을 통째로 꺼도 78까지밖에 안 내려가고,
환경광만 0.34 → 0.14로 내리면 101 → 95로 거의 안 움직인다. 대신 **플레이어 광원을 끄면 101 → 48**로
반이 날아간다(같은 값이 2막은 44 → 22). 즉 조명 세기는 막마다 비슷한데 **석재가 밝아서
횃불이든 포스트 바닥값이든 그만큼 세게 되돌아오는 것**이다(v742에서 원화의 아이보리를 맞추려고
바닥 `0x7a735c` → `0xb8ae90`, 벽 `0xa89f82` → `0xd6caa6`로 올렸다).
그래서 조명이 아니라 반사율을 내린다(`COR_LIT` 0.62). 색조는 그대로 유지된다.
**반사율만으로는 먼 곳이 안 죽는다** — 0.40까지 내려도 먼 구간이 41(2막 31)에 머문다. 남은 것은
포스트·안개 바닥값이므로 배경을 어둡게(`0x0c1517` → `0x090f10`) 하고 안개를 짙게(0.014 → 0.024)
해야 거리 감쇠가 생긴다. 최종 [39,59,55,40]으로 2막의 1.3배 — 3막이 가장 따뜻하고 밝은 막이라는
정체성은 남기고 던전으로 돌아왔다.
**이렇게 바꾸면 3막 캐릭터 팔레트가 같이 어두워진다**(부패한 왕 평균 휘도 95 → 72). 배경이 더
많이 어두워졌으므로 대비는 오히려 좋아지지만, 다시 역산할 때는 이 환경에서 재야 한다.

**막을 새로 채울 때 몬스터만 넣으면 절반만 한 것이다(v752 검토에서 나온 것들).** 3막을
몬스터로 채운 뒤 다시 훑어보니 이전 막에는 있는데 3막에만 없는 것이 셋 있었다.
· **보스 재료·대장간 장비.** `BOSS_MATERIAL`에 없는 보스는 잡아도 아무것도 안 떨어진다 —
  70·80·90층 보스가 그랬다. 재료 3종과 3막 장비 3종을 넣었다. **3막 무기를 1막과 같은
  치명타로 두면 그냥 상위호환이라 고를 것이 없다** — 슬롯이 하나뿐이므로 다른 능력치로 줄 것.
  `power`(공격력)·`hpmax`(최대 체력)는 **`applyMetaUpgrades` 한 곳에만 얹으면 된다** —
  이 게임의 모든 피해가 `player.atk`에서 파생되기 때문이다(타격 지점마다 곱할 필요 없음).
· **복도 매복.** 2막의 복도 함정 골렘을 3막에서 막으면서 그 자리가 비었다. 3막은 반대로
  '작은 것 여럿'(포자 벌레 한 무리)으로 채웠다. `corridorTrap` 각성 판정은 골렘 전용이 아니라
  타입과 무관하게 돈다.
· **번역.** 새 문자열은 `I18N_DICT.en`에 넣지 않으면 영어 화면에 한글이 그대로 뜬다.
  대장간 재료 이름·장비 이름·능력치 라벨·보스 패턴 토스트가 전부 여기에 걸린다.
또 하나: **난입 보스 토스트가 내부 id를 대문자로 찍고 있었다**(`BROODTICK`). 이름표가 여러 군데로
복사돼 있어서 `monsterName(type)` 하나로 합쳤다 — 새 몬스터를 넣을 때 고칠 자리가 한 곳이어야 한다.

**팔레트 역산은 뷰어가 아니라 게임 안에서도 할 수 있다(v751, 부패한 왕).** 병합
(`c3MergeStatic`) 뒤에는 메시 하나 = 재질 하나라, **다른 재질의 `colorWrite`만 끄고 깊이는 남겨**
가림을 유지한 채 기준 렌더와 다른 픽셀을 그 재질의 마스크로 잡을 수 있다. 지오메트리가 고정이면
마스크는 한 번만 만들면 되므로 이후에는 회차당 렌더 1회다(전체 42회로 끝난다).
· **광원·카메라는 `update()`가 매 프레임 플레이어 자리로 옮긴다.** 이걸 안 돌리고 재면 캐릭터가
  환경광만 받아 측정이 통째로 어두워진다 — 정식 역산된 탐식 구울조차 전 재질이 청록 [17,33,37]
  한 색으로 뭉쳐 나왔다. 90프레임쯤 굴려 자리를 잡은 뒤 rAF를 끊을 것(적은 `awake=false`로 세워 둔다).
· **목표는 '원하는 색'이 아니라 파이프라인이 낼 수 있는 색이어야 한다.** 원화의 차가운 라벤더를
  목표로 잡았더니 베이스를 순청(`0x2027ff`)까지 밀어도 파랑이 17/255 모자랐다 — 3막 포스트
  (대기 원근·안개)가 따뜻한 올리브로 물들여 파랑을 못 올린다. 3막 자체의 대비(따뜻한 크림 ↔
  산호빛 종양)로 목표를 다시 잡으니 16종 중 13종이 오차 ≤10에 들어왔다(평균 채도 0.093 → 0.247).
· **화면 픽셀이 적은 재질은 풀지 말 것.** 옷(159px)·가죽(8px)·근섬유(6px)는 역산이 레일에 붙어
  청록(`0x63d9ff`) 같은 위태로운 값이 나온다 — 푼 이웃 재질에 맞춰 손으로 고르는 쪽이 안전하다.
· **발광 재질 옆의 마스크는 블룸이 오염시킨다.** 녹청(왕관 보석)은 발광 결정 바로 옆이라
  베이스를 `0x010101`까지 내려도 출력이 137에서 안 내려갔다.
· 목표 대역은 감으로 잡지 말고 **이미 정식 역산된 캐릭터를 같은 자리에서 재서** 기준으로 쓸 것
  (탐식 구울: 재질별 휘도 56~78 · 평균 67 · 채도 0.148).

**캐릭터는 진짜 그림자를 안 진다 — 발밑 블롭이 대신한다. 새 리그를 쓸 때 이걸 어기기 쉽다.**
몬스터 빌더가 `grp.traverse(o => o.castShadow = false)`로 통째로 끄고 블롭을 붙이는 것이 규칙인데,
`buildArcherRig`만 헬퍼가 `o.castShadow = true`라 궁수 메시 87개가 전부 그림자를 졌다.
던전에서 특히 크게 보이는 이유는 그림자 광원이 **플레이어 바로 옆의 점광원(`torch`)** 이기 때문이다 —
광원이 가까우면 그림자가 그만큼 확대된다(실측: 기사 60×19픽셀 · 마법사 0 · **궁수 135×80**,
그림자 픽셀 수로는 342 / 0 / 2394).
비용도 컸다: 던전 한 프레임 드로우콜 1244 → 966, 삼각형 73,784 → 65,726.
점광원 그림자는 큐브맵이라 캐스터 하나가 여러 번 그려진다 — 메시 87개를 끄면 278콜이 준다.
찾는 법: `player.obj.traverse`로 `isMesh && castShadow`를 세면 기사 4 · 마법사 4 · 궁수 87로 바로 드러난다
(남은 4개는 숨겨진 기사 메시다).

**픽셀 크기는 `PIXEL_SCALE`이 아니라 `PX_FIXED_H`가 정한다.** `PIXEL_SCALE`(=2)은 `?px=0`
예전 모드 전용이라 지금 배포되는 화면에는 아무 영향이 없다 — 여기를 고치면 아무 일도 안 일어난다.
픽셀아트 모드는 세로를 `PX_FIXED_H`로 고정하고 가로만 화면비로 유도해 기기와 무관하게 같은
픽셀 크기를 낸다. 720 기준으로 환산하면 **360 = 배율 2, 320 = 2.25, 288 = 2.5, 240 = 배율 3**이다
(v739에서 240 → v740에서 288 → v753에서 320 — 전부 사용자 지시).
**포스트 패스 비용은 여기에 안 걸린다** — 합성 쿼드는 캔버스 해상도에서 돌기 때문이다
(실측: 360→240으로 줄여도 포스트는 103ms 그대로, 프레임 전체는 590 → 517ms).

**SSAO는 렌더 타깃을 새로 안 만들어도 된다 — 깊이·노멀이 이미 있다.** 구름 그림자용 깊이
텍스처와 모서리 하이라이트용 노멀 버퍼가 포스트에 이미 들어와 있어서 그 둘만으로 화면공간 AO가
나온다. **노멀 버퍼는 `MeshNormalMaterial`이 채우므로 뷰 공간이다** — 위치도 뷰 공간으로 맞춰야
하고(`uInvP`/`uProj`), 구름 그림자가 쓰는 `uInvVP`(월드)를 그대로 쓰면 안 된다.
**피부·식생 픽셀은 건너뛸 것** — 그 자리에는 진짜 노멀이 아니라 표식색(빨강·초록)이 들어 있다.
바닥·벽은 정상 계산되므로 캐릭터 발밑 접지 그림자는 그대로 남는다.
**AO는 외곽선·모서리 하이라이트보다 먼저 곱한다.** 나중에 곱하면 이미 어두워진 외곽선을 한 번 더
눌러 경계가 새까맣게 뭉갠다(둘 다 같은 자리를 어둡게 한다).
**난수 회전을 넣지 말 것.** 카메라를 텍셀 격자에 스냅하므로 시간축 누적이 어긋나 노이즈를 지울
수단이 없다. 황금각 나선으로 결정적인 표본을 쓰는 쪽이 지글거림보다 낫다.

**AO를 표본 거리로 가중하지 않으면 어두워짐이 사방으로 번진다.** AO는 광원 방향이 없어서
물체 밑동을 빙 둘러 어둡게 한다 — 여기에 반경 안의 모든 가림을 같은 무게로 세면 잔디밭·포장
바닥까지 넓게 지저분해진다(실측: 화면의 30.3%가 평균 33.3/255). 표본까지의 실제 거리로
`(1 - d/R)²`을 곱하면 밑동에 붙는 접촉 그림자가 되고 **범위가 절반으로 준다**(14.1% / 평균 20.6).
**반경을 줄이는 걸로는 안 고쳐진다** — 반경을 2.5→0.8로 3배 줄여도 30.3%→26.1%뿐이었다.
가중치를 넣으면 표본 합이 작아지므로 **세기를 5배쯤 올려야 같은 깊이가 나온다**(1.2 → 5.0).
정규화는 가중치 합이 아니라 고정 표본 수로 할 것 — 가중치 합으로 나누면 먼 가림만 있어도
완전히 어두워져 거리 가중치가 무의미해진다.
잔디 탓이 아니다: `?pgrass=0`(예전 3D 풀잎)로 바꾸면 오히려 26.1%→35.4%로 늘어난다.

**"건물 하단이 검게 반짝인다"는 AO가 아니었다 — v734를 넣었다가 v736에서 되돌렸다.**
증상을 AO 표본 뒤집힘으로 보고 두 가지를 고쳤었다(① 가림 판정 0/1 → `smoothstep` 완만화,
② 접선 기저의 `abs(n.z) < 0.99` 분기 → 무분기 Duff 기저). 얼룩 지표는 실제로 좋아졌지만
(이웃 픽셀 차 0.15 초과: 16.9% → 7.9%) **사용자가 본 깜빡임은 그대로였다.** 진짜 원인은
대장간 기단의 면 겹침이었다(아래 항목). AO 코드는 v733 상태로 되돌렸으니 다시 손대지 말 것 —
같은 증상이 또 보이면 **AO를 껐다 켜서 갈라 보는 것이 먼저다**(그 벽에서는 AO 켬/끔의
밝기가 소수점까지 같았다. 즉 AO는 애초에 그 자리를 건드리지도 않았다).
참고로 그때 같이 재 둔 것: 표본을 10 → 16으로 늘리면 비용이 111.6 → 150.6ms인데 얼룩은
7.6%로 오히려 나빴다. **표본을 늘리는 건 어느 쪽으로도 답이 아니다.**

**AO 얼룩 측정은 색 양자화·디더링에 오염된다.** AO 항을 '켠 밝기 / 끈 밝기'로 뽑으면
`uQuant`·`uDither`가 만든 베이어 점 패턴이 그대로 섞여 들어온다. 절대값은 과장돼 있으니
**같은 방식으로 잰 값끼리의 비교로만** 쓸 것.

**AO를 건물·벽에만 걸려면 노멀 버퍼 알파에 표식을 하나 더 넣는다.** 포스트는 깊이만으로는
'이 픽셀이 건축물인지'를 모른다. 알파를 세 값으로 쓴다 — **식생 0.0 / 건축물 0.5 / 그 외 1.0**.
0.5를 고른 이유: 외곽선 스위치가 `step(0.5, a)`라 0.5는 통과한다(건축물 외곽선은 그대로 남는다).
RGB는 건드리지 않으므로 모서리 하이라이트도 유지된다.
표식은 피부 패스와 같은 요령이다 — `_hideNonSolid`가 1패스에서 빼고 전용 패스에서 다시 그리므로
**드로우콜이 안 늘어난다**(실측: 마을 프레임 전체 3444 → 2853. 오히려 줄었는데, 1패스에서 숨긴
객체가 그 패스의 그림자맵 갱신에서도 빠지기 때문이다 — 색 패스가 매 프레임 그림자를 다시 굽기
때문에 화면에는 영향이 없다).
**가림 판정은 표본마다 `tNormal.a`를 한 번 더 읽어야 한다** — 텍스처 조회가 표본당 1회에서 2회로
늘어(10 → 20) 포스트가 105.8ms → 111.6ms가 된다.
표식 대상은 `markArch()`로 붙인다: 마을은 건물·성벽·성문·풍차·던전 입구, 던전·성은 `wallGroup`
통째로. **표식만 켜고 AO를 꺼도 그림이 완전히 같지는 않다**(마을 0.10%·최대 64) — 1패스에서
숨긴 건축물 뒤의 것이 노멀 버퍼에 먼저 찍히고 전용 패스가 덮으면서 실루엣 가장자리 한 픽셀이
달라지기 때문이다.

**'벽 밑동에 AO가 안 보인다'는 세기 문제가 아니었다 — 가림 판정에서 바닥이 빠져 있었다.**
건축물 전용 판정(`uAOArch`)은 **가리는 쪽**만 본다. 그런데 벽 밑동을 어둡게 하는 것은 바닥이고,
바닥에는 건축물 표식이 없다 — 그래서 벽면 AO 항이 **세기와 무관하게 정확히 1.000**이었다
(실측: 벽 높이 0.1~2.0에서 세기 5·9·13·18 전부 1.0. 반대로 벽에 붙은 바닥은 세기 5에서 이미 0.79).
**받는 쪽이 건축물이면 가림은 아무거나 인정**하도록 한 줄을 더하면(`recvArch`) 벽 밑동이
0.795(세기 5) → 0.521(세기 13)로 내려온다. 벽↔바닥 양쪽이 대칭이 되는 것이 맞다.
**비용은 오히려 준다** — 건축물 픽셀은 표본마다 `tNormal`을 다시 읽을 필요가 없어져
표본당 조회가 2 → 1로 돌아간다(포스트 실측 103.0 → 100.8ms).
**단면으로 값을 고를 것.** 반경 1.6은 밑동 그림자가 0.4 안에서 끝나 거의 안 보이고,
3.6은 벽 중턱까지 번지는데 단면이 단조롭게 안 떨어져 얼룩진다(0.63→0.50→0.73→0.80).
2.4가 깨끗하게 떨어진다. **세기는 5.0이 기본이다** — v737에서 12로 올렸다가 v738에서 되돌렸다
(사용자 지시. 12는 밑동 항이 0.52까지 내려가 진하다). 반경 2.4는 그대로 두었다.

**AO 효과는 막마다 완전히 다르다 — 던전에서는 거의 안 보인다.** 실측(세기 1.2·반경 2.5):
마을은 화면의 28.8%가 평균 34.8/255 어두워지는데, 던전(7층)은 세기를 1.8·반경 4.0까지 올려도
17.3%가 평균 8.5/255뿐이다. 던전이 워낙 어둡고(`ambientLight` 0x12140c) 바닥이 평평한 데다
발밑 블롭 그림자가 이미 접지를 그려 주기 때문이다. **비용은 어디서나 똑같이 든다** —
포스트 패스만 격리해 재면 54.1ms → 105.9ms로 거의 두 배다(SwiftShader 900×600. 소프트웨어
래스터라이저라 절대값은 무의미하고 **비율만** 의미가 있다).
비용을 아끼려면 던전에서 `uAOStr`을 0으로 두는 편이 낫다(`uCloudStr`을 던전에서 0으로 두는 것과 같은 요령).

**포스트 패스 비용은 `gl.finish`로 못 잰다.** WebGL의 finish는 여기서 안 막혀서 0.01ms 같은
말도 안 되는 값이 나온다. `readPixels`로 1픽셀만 읽어 GPU를 강제 동기화할 것 — 그래야
54ms/106ms처럼 재현되는 값이 나온다(같은 설정 5회 반복의 산포가 ±2ms).

**불규칙한 외곽선은 반지름을 정규화해야 자리를 안 벗어난다.** 물웅덩이는
`r(θ) = R·(1 + Σ aₖ·cos(kθ + φₖ))`로 만드는데(2·3·5엽), 진폭과 늘림이 `R` 위에 **곱으로** 얹혀서
그냥 두면 가장 먼 점이 `R`의 1.9배까지 자란다. 타일 반폭이 `TILE/2`=2.5인데 3.6까지 뻗어
테두리 돌이 벽 속에 박혔다. 각도를 훑어 최대 반지름을 구하고 그게 정확히 `R`이 되게 맞추면
예전 원판과 같은 자리를 차지한다(실측: 수면 2.01·돌 2.34로 둘 다 2.5 안).
**정규화하면 넓이가 준다** — 길쭉할수록 심하다. 진폭·늘림을 낮춰 되찾을 것
(실측 넓이 중앙값: 예전 원판 8.45 → 진폭 큰 첫 판 5.79 → 조정 후 6.77, 반지름비는 1.59로 유지).
**테두리 돌은 반드시 같은 외곽선 위에 놓는다.** 원둘레에 놓으면 돌이 다시 원을 그려서
웅덩이만 고쳐 봐야 소용이 없다. 모양 판단은 게임 스크린샷보다 **외곽선만 위에서 내려다보게
여러 개 한 장에 그려 보는 쪽**이 빠르다(게임 화면은 iso 각도·픽셀화·가림 때문에 오판하기 쉽다).

**원거리 캐스터의 유지 거리는 발사 사거리와 짝이다.** `MAGE_KEEP`(유지)만 늘리고
`MAGE_RANGE`(발사)를 그대로 두면, 물러난 자리에서 못 쏘니까 결국 다시 붙으러 들어온다.
반드시 `RANGE > KEEP + KEEP_BAND`여야 한다(현재 12 > 9 + 1.25). 화염구는 속도 11·수명 3.0이라
33까지 날아가므로 사거리를 늘리는 쪽은 여유가 많다.
**멀어질 땐 포위 지점이 아니라 플레이어 반대 방향으로 곧장 물러나야 한다.** 플레이어(6.5)가
캐스터(~3)보다 두 배 빠른데 접선 방향으로 비스듬히 가면 도망 속도를 그만큼 까먹는다.
카이팅 허용 폭(`KEEP_BAND`)도 좁혀야 한다 — 넓으면 그 폭만큼 안쪽에 서는 개체가 생긴다
(실측: 폭 2.0일 때 정지 상태 최소 거리 4.3, 폭 1.25 + 직선 후퇴로 6.7).
혼령(`wraith`)은 자체 히트앤런 상태기계(`hrState`)에서 `continue`하므로 이 값들과 무관하다.
보스 캐스터(시종장·얼음 여왕)도 `isRanged`가 아니라 영향받지 않는다.

**Playwright에서 페이지를 놀리면 rAF가 스로틀링돼 행동 계측이 통째로 틀어진다.**
`waitForTimeout(10000)`으로 10초를 그냥 기다리며 발사 횟수를 셌더니 예전 22회 / 새 값 0~11회로
나와서 회귀로 오판할 뻔했다. 같은 창을 100ms마다 `page.evaluate(() => 0)`으로 깨워 주고 다시
재니 **양쪽 다 110회, 플레이어 피해도 1505로 완전히 동일**했다. 시간이 흐르는 동안의 행동을
잴 때는 A/B 양쪽을 같은 주기로 폴링할 것.

**`flatShading: true`가 지오메트리 노멀을 통째로 무시한다.** 이 게임 캐릭터는 전부
`getLowPolyMat`(=`flatShading: true`)이라, 지오메트리 노멀을 아무리 손봐도 색 패스는 면 노멀을
다시 만들어 쓴다. 노멀을 부드럽게 하려면 그 머티리얼의 `flatShading`도 같이 꺼야 한다.
덤으로: r128 `PolyhedronGeometry`는 detail>0이면 이미 구면(부드러운) 노멀을 갖고 있다
(detail 0만 면 노멀). 즉 몬스터 머리(20면체 detail 1)는 `flatShading`만 꺼도 부드러워진다.

**`BufferGeometryUtils`는 동봉돼 있지 않다**(`mergeVertices` grep 0건). 정점 병합·노멀 평균이
필요하면 위치를 양자화해 직접 묶어야 한다(`_smoothSkinGeo` 참고).

**`cullSceneLights`의 기준점은 카메라가 아니라 플레이어다.** 카메라는 플레이어에서 25칸쯤
떨어져 있어서, '카메라와 플레이어 사이에 있는 벽 횃불'이 내 횃불보다 가깝다고 판정된다.
그래서 걷는 동안 플레이어 광원이 깜빡깜빡 꺼졌다(실측: 3~85층에서 걷는 시간의 5~15%).
플레이어 광원(`torch`/`nearLight`)은 아예 경쟁에서 빼고 슬롯을 예약할 것.

**컬링 판정은 `_culled` 플래그가 아니라 `visible`로 할 것.** 컬링이 끈 광원을 다른 코드가
다시 켜면 (`_culled=true` & `visible=true`) 상태가 되고, `!_culled`로만 보면 그 광원을 건너뛰어
계속 켜진 채 남는다 — 동시 점등 수가 한도를 넘는다(실측 3층에서 6개여야 할 게 7개).
이 수가 흔들리면 three가 머티리얼을 전부 재컴파일한다(아래 항목 참고).

**점광원 상한을 새로 만들지 말 것.** `cullSceneLights(camera)`/`MAX_DYN_LIGHTS`(6)가 이미 있고,
코드 주석 그대로 "렌더 실패(흰 화면)를 막기 위해" 존재한다. 이걸 모르고 별도 상한(`?plcap`)을
넣었다가 두 시스템이 매 프레임 서로의 `visible`을 덮어써서 켜진 광원 수가 6 고정 → 9~16으로
흔들렸고, 개수가 바뀔 때마다 three가 머티리얼을 전부 재컴파일해 셰이더 프로그램이 51→65개로
불어났다. 실제 기기에서 2막 진입 시 컨텍스트 손실 + 흰 화면으로 터졌다(v702에서 제거).

**블룸 임계는 막별로 다르다.** `bloomMat.uniforms.uThresh`는 유니폼 하나이고 각 막 분기가
따로 대입한다 — 1막·3막 0.62, **2막만 0.70**. 2막에만 달빛(방향광)이 있어 금속 갑옷 하이라이트가
임계를 넘어 캐릭터만 유독 번지기 때문이다. 전역으로 올리면 안 된다: 이 게임의 블룸은 임계 바로
위 좁은 구간에 몰려 있어서, 0.66이면 1막 블룸은 4.86%→0.04%로 사라지는데 2막은 1.09%가 남는다
(1막이 먼저 죽는다).

## CrazyGames 준수사항 — 되돌리지 말 것

- 광고 중 게임 오디오는 반드시 음소거(`SFX.suspendForAd()` / `resumeAfterAd()`)
- 일반 접속에서 치트 버튼(`bossTestBtn`/`maxLevelBtn`) 노출 금지
- 미드게임 광고는 자체 하한 2분

## 켜지 않기로 한 것

`?toon=N`(조명 계단화)은 두 번 구현하고 두 번 껐다. 선형 밝기로 자르면 아무 일도 안 일어나고
(확산광이 0~0.2에 몰려 있어 전부 한 칸에 떨어진다), 감마 공간으로 고쳐도 띠가 생기는 대신
화면 전체 노출만 오르내린다. 조명이 너무 균일해서 그렇다 — 켜려면 조명 설계부터 다시 짜야 한다.
코드에 이유가 주석으로 남아 있으니 지우지 말 것.

**골렘 시드에 `Date.now()`를 다시 넣지 말 것.** "개체마다 다른 모양"은 좌표가 이미 보장한다
(골렘은 최소 3칸 간격, 131·977이 서로소라 40칸 맵 안에서 시드가 겹칠 수 없다). `Date.now()`는
거기에 기여하지 않으면서 골렘이 나오는 45층 이상의 렌더 A/B 비교만 불가능하게 만들었다.
지금은 층번호를 섞어 층마다 다르면서도 재현 가능하다.

## 캐릭터 모델링은 `viewers/`에서 먼저

게임에 바로 넣지 않고 캐릭터 하나만 띄우는 뷰어에서 다듬는다. 게임과 같은 렌더 파이프라인
(픽셀화 + 깊이 외곽선 + ACES)과 같은 머티리얼 헬퍼를 쓰므로 뷰어에서 본 실루엣이 게임에서도 같다.
새 캐릭터는 기존 뷰어를 복사해 몸통 부분만 갈아 끼운다. 자세한 것은 `viewers/README.md`.

뷰어의 three.js는 CDN이 아니라 `viewers/three.r128.min.js`(게임에 동봉된 것과 같은 파일)를 쓴다.
작업 환경 브라우저는 CDN에 못 나가므로 CDN을 걸면 렌더 검증 자체가 불가능하다.

렌더 환경은 게임에서 통째로 옮겨 왔다 — 저해상도 버퍼·톤매핑·조명 셰이더 청크·노멀 패스·블룸·
포스트 셰이더·픽셀 스냅·막별 환경값·조명 리그가 전부 같은 값이다. 같은 회색 구를 양쪽에 놓고
재서 확인했다(휘도 차 0.5%, FOV 소수점 4자리 일치). **뷰어의 렌더 상수를 '보기 좋게' 바꾸면
뷰어가 거짓말을 하기 시작한다.** 특히 세 가지를 지킬 것:
· 광원은 캐릭터가 아니라 3칸 앞 '플레이어 자리'에 붙인다(게임의 빛은 전부 플레이어에게 달려 있다).
· 확대는 카메라를 당기지 말고 FOV를 좁힌다(깊이 램프 20~33 밖으로 나가면 밝기가 달라진다).
· 카메라 거리 26.46은 잰 값이다(플레이어 25.399 / 근접 교전 중인 몬스터 26.46).

**색은 감으로 찍지 말고 역산할 것.** 프로브 구로 재 보면 베이스 `0x60` 위는 출력이 사실상
평평하다(0x60→116, 0xf0→132) — 근접 거리에서 광원이 세서 그 위가 전부 클리핑된다. 거기에
포스트 체인이 채도를 상수로 깎는다(대기 원근이 항상 최대라 −31.5%, 안개가 36% 더 당김).
그래서 **원화의 밝은 색을 그대로 넣으면 8단 그라데이션이 통째로 같은 회색으로 뭉갠다**
(구울 실측: 예전 팔레트 `0x69~0xff` → 출력 `#74746c~#7e7e72`). 역산한 팔레트는 `0x16~0x8c`이고
평균 채도가 0.108 → 0.172로 올랐다. 절차는 `viewers/README.md`의 '색 고르기' 절.

**위를 향한 면이 어두운 것은 색 문제가 아니다.** 던전 광원은 전부 플레이어에게 달려 있고
전방·아래쪽이라 어깨 윗면은 반구광만 받는다. 베이스를 ×1.8 올려도 휘도 24→27.5인데 채도만
0.32→0.21로 무너지고, 모델을 0.65배로 줄여도 24→30이다. 발광 악센트나 실루엣으로 풀 것.

사용자에게 뷰어를 보낼 때는 `node viewers/build-standalone.js viewers/<파일>.html`로
three.js를 인라인한 단일 파일을 만들어 보낸다(파일 하나만 받아 열어도 동작해야 한다).

## 잡동사니

- 임시 디버그 스크립트를 저장소에 만들지 말 것(과거에 `dbg2.js` 등이 섞여 들어간 적 있다)
- 사용자는 주로 한국어로 소통한다. 코드 주석도 한국어다
- 사용자는 모바일에서 작업하는 경우가 많다 — 파일을 주고받기보다 저장소에 올리는 쪽이 다루기 쉽다
  (작업 브랜치에 올려 검증 결과를 보고하고, 머지 지시를 받으면 `main`에 반영 — 위 릴리스 절차 참고)
