# Dungeon Legion — 3막~5막 기획서

> **문서 목적**: 3·4·5막 작업 지시서.
> **기준 버전**: `index.html` = **v759** (3막 완료 · 90층 더블 보스)
> **직전 개정**: v289(2막 완료) 기준 초판 → 3막을 실제로 구현하면서 달라진 것을 반영.
> **대상**: 코딩 담당. 이 문서 + `CLAUDE.md`를 읽고 작업을 시작할 수 있게 작성됨.

**이 문서와 `CLAUDE.md`의 역할 분담**
- **이 문서** = *무엇을 만들 것인가*(기획·컨셉·라인업·밸런스·마일스톤)
- **`CLAUDE.md`** = *어떻게 만들 것인가*(저장소 규칙·렌더 파이프라인·반복해서 발목 잡은 함정)
  실제 작업 규칙은 전부 `CLAUDE.md`가 최신이다. 충돌하면 `CLAUDE.md`를 따를 것.

---

## 0. 초판에서 바뀐 것 (요약)

초판은 "2막까지 구현된 상태"를 전제로 쓰였다. 그 사이에 3막이 통째로 들어갔고,
**기획과 다르게 구현된 부분이 있다.** 4막을 시작하기 전에 이 절만은 반드시 읽을 것.

| 항목 | 초판 기획 | 실제 구현(v758) |
|---|---|---|
| 3막 상태 | ❌ 미착수 | ✅ 완료 |
| 2막 보스 | ⚠️ 전부 구울로 통일(미완) | ✅ 시종장 망령·흑기사·얼음 여왕 + 전용 패턴 |
| 70층 보스 | 탐식 구울 | **포란충**(`broodtick`) |
| 80층 보스 | 산모(Brood Mother) | **탐식 구울**(`gluttonghoul`) |
| 90층 보스 | 부패의 심장 + 타락한 왕 (**더블 보스**) | **더블 보스로 확정**(v759) — 심장은 공격 패턴 없이 왕을 부활시킨다 |
| 3막 원거리 몹 | 종양 술사(`tumorcaster`) | **농침벌**(`stingwasp`) — 공중 독침 캐스터 |
| 3막 근접 기본 | 부패한 구울(61~) | **포자 벌레**(`sporeling`, 61~) / 부패한 구울은 71~ |
| Three.js | CDN r128 | **파일에 동봉된 r128**(CDN 의존 제거) |
| 파일 관리 | `/mnt/user-data/outputs`에 `dungeon_vNNN.html` | **git 저장소**. `index.html` + `index-vNNN.html` 두 벌 |
| 렌더 | 저해상도 + 외곽선 | **픽셀아트 모드 기본**(`PX_FIXED_H` 320) · SSAO · 노멀 버퍼 표식 · 블롭 그림자 |

**더블 보스 설계가 한 번 바뀌었다.** 처음 구현(v750~v758)에서는 심장을 뺐다 — 심장이 별도
공격 패턴을 가지면 화면에 읽을 것이 두 배가 되는데, 아이소메트릭 + 픽셀아트라 동시에 두 개를
읽히기 어렵기 때문이다. v759에서 **심장이 공격 패턴을 갖지 않는 형태**로 다시 넣었다.
읽을 것은 여전히 왕 하나이고, 심장은 전투 상대가 아니라 **목표물**이다. 자세한 것은 5-4절.

---

## 1. 현재 구현 상태

| 막 | 층 | 테마 | 상태 |
|---|---|---|---|
| 1막 | 1~30 | 이끼 동굴 (자연) | ✅ 완료 |
| 2막 | 31~60 | 무너진 성 (붕괴한 문명) | ✅ 완료 (보스 3종 + 전용 패턴 포함) |
| 3막 | 61~90 | 부패 (병든 유기체) | ✅ 완료 |
| 4막 | 91~120 | 지옥 (작열하는 과잉) | ❌ 미착수 |
| 5막 | 121~150 | 심연 (텅 빈 결핍) | ❌ 미착수 |

**막 사이에는 "여행자의 쉼터"** 허브. 좌측 동굴로 등장 → 시장 → 우측 동굴로 다음 막 진입(3초 게이지).
90층(현재 콘텐츠의 끝)을 클리어하면 `restIsFinal = true`인 쉼터로 가고, 출구 패드가 붉게 막히며
길잡이 NPC가 "아직 열리지 않았다"고 안내한다. **4막을 넣을 때 이 분기를 푸는 것이 첫 작업이다.**

### 현재 게임 전체 구조 (v758)
- 마을(상점·대장간·스킬 교관) → 던전 1~30 → 쉼터 → 31~60 → 쉼터 → 61~90 → 쉼터(막힘)
- 마을 포탈로 해금된 막(2막=31 / 3막=61)에 바로 진입 가능(`restChoiceOverlay`)
- 직업 3종(기사·마법사·궁수), 대장간 장비(막별 3종), 메타 업그레이드
- CrazyGames 제출 준비 중 — 준수사항은 `CLAUDE.md`의 해당 절 참조

---

## 2. 코드 앵커 (v758 기준)

`index.html` 단일 파일. **앞부분은 동봉 라이브러리라 게임 코드는 4076줄 이후**다.

```bash
awk 'NR>4076' index.html | grep -n "찾을것"     # 게임 코드만 검색
```

| 기능 | 함수/상수 | 줄(절대) |
|---|---|---|
| 플래그 정의 블록 | `AD_TEST`/`DEV_MODE`/`PX_MODE`/`GAME_VERSION`/`ACT3_MOBS`/`B3_PAT`/`GG_FLEX` | 4143~4180 |
| 번역 사전 | `I18N_DICT` | 4083 |
| 투시 셰이더 | `applyCutoutShader(material)` | 7304 |
| 피부 재질 표식 | `markOrganic(mat)` | 7395 |
| **막 판정** | `currentAct()` | 7676 |
| 2막 메시 빌드 | `buildCastleMesh(dgn)` | 8067 |
| 3막 팔레트 | `const COR` / `COR_V2` / `COR_LIT` | 8154 / 8170 / 8186 |
| 3막 메시 빌드 | `buildCorruptMesh(dgn)` | 8659 |
| 던전 생성(크기/방) | `generateDungeon()` | 8883 |
| **막별 분기 진입점** | `buildDungeonMesh(dgn)` | 8971 |
| 3막 드로우콜 병합 | `c3MergeStatic(root, skip)` | 13068 |
| 3막 보스 행동 선택 | `c3Pick([[이름, 가중치], …])` | 12259 |
| 3막 몬스터 빌더 | `buildSporelingBody` 13160 / `buildStingwaspBody` 13328 / `buildBroodTickBody` 13433 / `buildGluttonGhoulBody` 13646 / `buildCorruptKingBody` 14160 / `buildRotGhoulBody` 14686 / `buildFleshGiantBody` 14809 | |
| 몬스터 이름표 | `monsterName(type)` | 16138 |
| 몬스터 생성 | `createAdvancedMonster()` | 16142 |
| 근접/원거리 풀 | `meleePool()` / `rangedPool()` | 16516 / 16537 |
| 적 배치·보스 선택 | `spawnEnemies(dgn)` (안에 `let btype`) | 16574 |
| 장식 애니메이션 | `updateCavernDecor(dt)` | 17318 |
| 층 하강 | `descend()` | 21260 |
| 층 시작 | `startFloor()` | 21270 |
| 보스 재료 | `BOSS_MATERIAL` | 21372 |
| 대장간 장비 | `SMITH_ITEMS` | 21387 |
| 쉼터 씬 빌드 | `buildRestScene()` | 22293 |
| 쉼터 진입 | `enterRest()` | 22645 |

> 줄 번호는 v758 기준이다. **편집 전 반드시 `grep`으로 재확인할 것.**

### 핵심 상수·공식
```js
const TILE = 5;
const MAP_MIN = 18, MAP_MAX = 40;

const actStart   = Math.floor((player.floor - 1) / 30) * 30 + 1;  // 1, 31, 61, 91, 121
const floorInAct = player.floor - actStart + 1;                    // 1~30
const actIdx     = Math.floor((player.floor - 1) / 30);            // 0=1막 … 4=5막
```
막당 30층 고정. 이 공식 덕분에 **맵 크기·방 개수·몬스터 강화는 새 막에서 자동 적용**된다.

---

## 3. ⚠️ 4막 착수 전 반드시 고칠 하드코딩

초판이 지적한 6건 중 **3건은 아직 남아 있다.** 3막을 넣을 때 일반화 대신 "61을 한 줄 더" 붙였기
때문이다. 4막은 같은 방식으로 또 붙이지 말고 **여기서 일반화하고 시작할 것.**

### 3-1. `currentAct()` — 아직 3까지만 (❌ 남음)
```js
// 현재 (7676)
function currentAct() { if (!player) return 1; if (player.floor >= 61) return 3; return (player.floor >= 31) ? 2 : 1; }
// 변경
function currentAct() { return player ? Math.floor((player.floor - 1) / 30) + 1 : 1; }
```

### 3-2. `descend()` — 30·60·90이 각각 하드코딩 (❌ 남음)
현재 세 줄이 거의 같은 내용을 반복한다(`meta.restUnlocked` / `meta.act2Cleared` / `restIsFinal`).
막 경계를 `player.floor % 30 === 0`으로 묶고, **마지막 구현 막**을 상수 하나(`LAST_ACT`)로 두어
그 이상이면 `restIsFinal = true`가 되게 할 것. 150층은 엔딩 분기.

### 3-3. `continueFromRest()` / 포탈 행선지 (✅ 해결됨)
`restNextFloor` 전역 + `restChoiceOverlay`로 정리됐다. 해금 플래그(`meta.act2Cleared`)만
4막용으로 하나 더 추가하면 된다.

### 3-4. `generateDungeon()` — `actBaseRooms` (❌ 남음)
```js
// 현재 (8883 부근)
const actBaseRooms = (actStart === 61) ? 5 : (actStart === 31) ? 4 : 3;
// 변경: 테이블로 (아래 8-2 표)
```

### 3-5. 테스트 패널 층 이동 버튼 (✅ 61·70·80·90 추가됨)
4막 착수 시 91·100·110·120 버튼 추가(`data-f`, index.html 166~181줄 부근).

### 3-6. 막 전용 스폰 코드에 상한을 걸 것 (3막에서 실제로 터진 문제)
복도 함정 골렘이 `floor >= 45` 하나뿐이라 3막에도 계속 나왔다.
**새 막을 만들 때 이전 막 전용 스폰은 `<= 60` 같은 상한을 같이 걸어야 한다.**
`meleePool()`/`rangedPool()`/복도 매복/난입 보스 전부 확인할 것.

---

## 4. 새 막 추가 표준 절차 (v758 갱신판)

1. **팔레트 상수 정의** — `const XXX = { ... }`
2. **cutout 공유 재질 생성** — 벽·기둥·아치·잔해 등 수직 구조물 **전부**
   `const xxxMatWall = applyCutoutShader(getLowPolyMat(XXX.wall));`
3. **벽 높이 상수** — `XXX_WALL_H`
4. **에셋 빌더 함수** — 벽 타일 / 장식 / 광원 / 기둥 / 잔해 / 특수 오브젝트
5. **광원 배열 + 파티클** — `xxxLights[]`, `xxxParticles`(평소 `visible = false`)
6. **`buildXxxMesh(dgn)`** — 바닥 → 벽 → 복도 장식 → 방 디테일 → 광원 순.
   ```js
   const wxf = (gx) => (gx - MAP_W / 2) * TILE;
   const wzf = (gy) => (gy - MAP_H / 2) * TILE;
   ```
   벽마다 `walls.push({x, z, halfW: TILE/2, halfD: TILE/2})` 등록 필수.
   벽에 튀어나온 장식(바위 등)을 붙이면 **콜라이더가 아니라 장식 자체를 벽 안으로 밀어 넣을 것**
   (1막 `fitWallRocks` 참고 — 콜라이더만 얹으면 1칸 복도가 막힌다).
7. **`buildDungeonMesh()` 분기** — 배경색 / 안개 색·농도 / 환경광 / 달빛 / 반구광 / 파티클 토글
   → `buildXxxMesh(dgn); return;`
   - ⚠️ 조명은 전역 하나를 공유한다. **막마다 설정하고 이전 막 복귀 시 복원**할 것.
   - ⚠️ **밝기는 조명이 아니라 석재 반사율이 지배한다.** 3막에서 이걸 몰라 두 번 헛짚었다
     (`CLAUDE.md`의 "3막이 너무 밝다의 범인" 절).
   - `bloomMat.uniforms.uThresh`는 막마다 따로 대입한다(1·3막 0.62 / 2막 0.70).
   - 던전에서는 `uCloudStr = 0`, 필요하면 `uAOStr = 0`(비용 절약).
8. **건축물 AO 표식** — 벽 그룹에 `markArch()`
9. **`updateCavernDecor()` 애니메이션** — early-return 조건에 새 배열/파티클도 포함
10. **`generateDungeon()`** — `actBaseRooms` 테이블에 새 막 추가
11. **몬스터** — `viewers/`에서 먼저 만든다(`viewers/README.md`).
    - `createAdvancedMonster()`에 스탯 분기 + `buildXxxBody()` 외형 분기
    - 살·피부 재질에 `markOrganic()` (전용 빌더가 자기 재질을 만들면 직접 걸 것)
    - 색만 다른 재질은 `userData.mergeInto`로 묶고 `c3MergeStatic`으로 병합
    - `meleePool()` / `rangedPool()`에 층 조건 + **이전 막 상한**
    - 이름은 `monsterName(type)` 한 곳에만 추가
12. **보스** — `btype` 분기 / 행동은 `c3Pick` 가중 추첨(거리순 if 사슬 금지, 5-6절)
13. **보스 재료·장비·번역** — `BOSS_MATERIAL` / `MATERIAL_INFO` / `SMITH_ITEMS`(`act: N`) /
    `I18N_DICT.en`. **이 넷을 빼먹으면 보스를 잡아도 아무것도 안 떨어지고 영어 화면에 한글이 뜬다.**
14. **복도 매복** — 막마다 하나씩 성격을 다르게(2막=큰 것 하나 / 3막=작은 것 여럿)
15. **쉼터 변형** — 직전 막 흔적 반영(9절)
16. **검증** — `node --check` → 해당 층 진입 → 이전 막 복귀 시 조명/파티클 복원 → 모바일 프레임

---

## 5. 3막 — 부패 / 병든 유기체 (61~90층) ✅ 구현 완료

> **공포 축**: "여긴 정상이 아니다." 1~2막은 *장소*였지만 3막은 **병든 유기체**다.

### 5-1. 비주얼 (구현된 것)
- **따뜻한 크림 석재 ↔ 청록 그늘 ↔ 채도 높은 산호빛 종양**이 3막의 대비다.
- 벽돌은 폭·간격·돌출·기울기를 따로 흔들어 줄눈이 자로 잰 듯 보이지 않게(`makeCorruptWallTile`).
- 바닥 타일은 두께 0.55에 y를 ±0.055 흔들고 윗면만 사인파로 민다(`displaceFloorTop`).
  **타일 높이를 흔들면 그 위 장식도 같은 오프셋을 따라가야 한다**(`corTileY`).
- 종양(산호)은 알갱이를 작게·많이 만들어 한 지오메트리로 병합(`mergeCoral`). 그림자는 지지 않는다.
- 덩굴은 공중을 가로지르지 않고 **벽면·기둥·바닥에 붙는다**. 굵기는 끝으로 갈수록 가늘게
  (`makeTaperedTube`) — `TubeGeometry`를 그대로 쓰면 각목이 된다.
- 바닥 핏줄은 굽이치는 곡선 + 끝 가늘게. 곧은 원기둥으로 눕히면 성냥개비로 보인다.

### 5-2. 팔레트 (실제 값)
```js
const COR = {
  floor: 0xb8ae90, floorB: 0xa79d82, floorDark: 0x8b8470,   // 아이보리 석재(COR_V2)
  wall:  0xd6caa6, wallB: 0xc3b795, wallC: 0xb0a583, wallSh: 0x87816d,
  flesh: 0xa82c42, fleshB: 0x7d2036, fleshC: 0xc4485c,      // 진홍 살덩이
  pale:  0xe9bfc4, paleB: 0xd49fae,                          // 창백한 감염 분홍
  bruise: 0x5e3354, bile: 0x6d7c3c, vein: 0x8a2438,
  glow:  0xff9a68, glowB: 0xffc89a,
  coralA: 0x8e2438, coralB: 0xb0384e, coralC: 0xc4626e, coralD: 0xd18488,
};
```
석재는 런타임에 `COR_LIT`(기본 **0.62**)를 곱해 어둡게 쓴다. 색조는 유지된다.
배경·안개 `0x090f10` / 안개 농도 0.024 / 환경광 `0x585a4c` × 0.26 / 달빛 0.16 / 반구광 0.14.

> **초판의 `ROT` 팔레트는 폐기.** 원화 반영(v742)과 밝기 조정(v753)을 거쳐 위 값으로 확정됐다.
> 여기를 "보기 좋게" 고치지 말 것 — 3막 캐릭터 팔레트가 전부 이 환경에서 역산돼 있다.

### 5-3. 몬스터 라인업 (실제 구현)
| 층 | 타입 | 역할 | 비고 |
|---|---|---|---|
| 61~ | **포자 벌레** (`sporeling`) | 근접 다수 | 작고 빠름. 복도 매복·포란충의 새끼로도 쓰임 |
| 65~ | **농침벌** (`stingwasp`) | 원거리 | 공중 부양 독침 캐스터. 맷집 얇음 |
| 71~ | **부패한 구울** (`rotghoul`) | 근접 | 포자 벌레보다 단단하고 한 방이 무겁다 |
| 78~ | **살덩이 거인** (`fleshgiant`) | 근접 탱커 | 아주 느리고 아주 단단(2막 골렘의 자리). **방당 1~2로 제한** |

> 살덩이 거인은 방당 수를 안 묶으면 85층에서 17마리가 나와 전투가 늘어진다.

**복도 매복(3막)**: 2막의 함정 골렘("큰 것 하나") 대신 **포자 벌레 한 무리**("작은 것 여럿").
`corridorTrap` 각성 판정은 타입과 무관하게 돈다.

### 5-4. 보스 (실제 구현)
| 층 | 보스 | 타입 | 반지름 | 패턴 |
|---|---|---|---|---|
| 70 | **포란충** (`broodtick`) | 곤충·소환형 | 1.3 | 산란(새끼 최대 6) · 점액 분사(부채꼴 3발, 밀착 시 1발) · 몸통 찍기 |
| 80 | **탐식 구울** (`gluttonghoul`) | 근접 압박 | 2.4 | 내려찍기 · 삼키기 돌진 · **HP 50%에 배 개방** → 위액 분사 |
| 90 | **부패의 심장 + 부패한 왕** (`rotheart` + `corruptking`) | 더블 보스 | 2.2 / 1.0 | 아래 참조 |

- 몸집이 실물 크기라 `C3_BOSS_RADIUS`로 충돌 반지름을 따로 곱한다.
- **탐식 구울(r=2.4)은 벽에서 2.4 안쪽 띠에 걸어서 못 닿는다**(플레이어가 설 수 있는 바닥의
  85~86%만 커버). 그래서 삼키기 돌진이 그 띠를 덮어야 한다(5.4칸 이동).
- 탐식 구울은 패턴마다 부위가 변형된다(v758): 내려찍기=팔 1.42배 · 삼키기=머리 1.34배+앞으로
  0.30 · 위액=배 1.26배. `?ggflex=0`으로 끔.

**90층 더블 보스 — 부패의 심장 + 부패한 왕 (v759 확정)**

심장은 **공격 패턴이 없다.** 벽에 박혀 뛰기만 하고, 왕이 살아 있는 동안 각질 껍질이 닫혀 무적이다.
왕을 쓰러뜨리면 죽지 않고 무릎 꿇으며 껍질이 열린다 — 그 **취약 창** 안에 심장을 부수지 못하면
심장이 왕을 되살린다.

| 단계 | 내용 |
|---|---|
| 평상시 | 심장 무적(껍질 닫힘). 왕만 상대한다 — 읽을 것은 하나뿐 |
| 왕 HP 0 | 죽지 않고 무릎 꿇는다. 심장 껍질이 열리고 왕은 무방비(피해도 안 들어감) |
| 취약 창 | **10초**(부활할수록 12 → 14 → 16초). 이 동안만 심장에 피해가 들어간다 |
| 창 종료 | 왕이 되살아난다 — 체력 **45% → 30% → 20% → 14%**로 점점 약하게 |
| 심장 파괴 | 왕도 같이 끝난다. 3막 클리어 |

- **체력 배분**: 왕 초기 = 단독일 때의 45%(9,430) / 심장 = 단독 왕 체력의 24%(5,029).
  실측(같은 공격력·같은 방): 왕 단독 58.6초 vs 더블 보스 55.5초 — **취약 창 2회 · 부활 1회**.
- **플레이의 핵심**은 "왕을 어디서 눕히느냐"다. 심장에서 먼 곳에서 눕히면 달려가는 데 창을 다 쓴다.
  창 7초 · 심장이 4.5칸 떨어진 판에서는 11번의 창 동안 심장이 18%밖에 안 깎였다(실측).
- **자비 규칙**: 심장을 못 알아챈 플레이어가 14%짜리 왕과 무한히 싸우지 않도록 창이 점점 길어진다.
- `?heart=0`으로 v758(왕 단독) 동작으로 되돌릴 수 있다.

**부패한 왕 디자인 확정 사항** (원화 반복으로 정해짐 — 구현도 이 방향):
- 괴물이 아니라 **영웅적 체격의 아름다운 변이체**
- **금속이 아닌 유기적 외골격 갑옷** — 젖고 번들거리는 생체 조직 질감
- 중년의 성숙한 얼굴, 얼굴 일부만 부패 / 두꺼운 어깨 갑옷 볼륨
- 부러진 황금 왕관 · 부패한 황금 한손검(오른손) · 카이트 방패(왼팔)
- 오른 어깨의 오염원 결정이 발광(핵 11개는 매 프레임 정점이 바뀌어 병합에서 제외)

### 5-5. 보스 재료·대장간 장비 (3막)
| 보스 | 재료 | 장비 | 슬롯 | 능력치 |
|---|---|---|---|---|
| 포란충 | 포란충의 각질판 🪲 | 포란충 각질 갑주 | 방어구 | `defense` +6%/레벨 |
| 탐식 구울 | 탐식의 어금니 🦷 | 탐식의 어금니 | 무기 | `power` +5%/레벨 |
| 부패한 왕 | 부러진 왕관 조각 👑 | 부패한 왕관 | 장신구 | `hpmax` +6%/레벨 |

> **막 장비를 이전 막과 같은 능력치로 두면 그냥 상위호환이라 고를 것이 없다.**
> 슬롯이 하나뿐이므로 막마다 **다른 능력치**를 줄 것. `power`·`hpmax`는 `applyMetaUpgrades`
> 한 곳에만 얹으면 된다(모든 피해가 `player.atk`에서 파생된다).

### 5-6. 보스 행동 선택 규칙 (4·5막에도 그대로 적용할 것)
**거리순 if 사슬로 고르면 안 된다.** 보스는 계속 붙으러 오므로 쿨이 도는 시점엔 이미 근접
사거리 안이고, 그 뒤의 조건은 영원히 안 걸린다(실측: 점액 0회 / 삼키기·위액 0회 / 방패 210초 중 1회).
`c3Pick([[이름, 가중치], …])`로 **가중 추첨**하고 **가중치만 거리로 흔들 것**.

같이 지킬 것:
- **돌진기는 실제로 몇 칸을 덮는지 재 보고 선택 조건을 정한다.** 삼키기는 실이동 1.7칸인데
  조건이 9칸이라 전부 빗나갔다.
- **'가끔 터지는 강화'에 영구 버프를 넣지 말 것.** 전환 1회 플래그로 묶는다(왕은 이걸 빠뜨려
  120초에 14번 터져 공격력 8배·속도가 플레이어보다 빨라졌다).
- **밀착 피해를 같이 확인한다.** 패턴을 늘리면 부채꼴 투사체가 같은 자리에 다 꽂힌다.
- **'유휴 70%'는 죽은 시간이 아니다** — 그 구간에 기본 근접 공격이 돈다. 쿨을 더 깎지 말 것.

---

## 6. 4막 — 지옥 / 작열하는 과잉 (91~120층) ❌ 미착수

> **공포 축**: **감각의 폭격**. 1~3막의 점점 어두워지던 흐름을 **여기서 확 뒤집는다.**
> 갑자기 밝고 타오르는 곳. 그 반전 자체가 충격.

### 6-1. 비주얼 방향
- 용암 강·용암 웅덩이 (발광, 흐름 애니메이션)
- 각진 흑요석·현무암 기둥 (검게 그을린 바위)
- 갈라진 지면 틈에서 잔불이 새어나옴
- 치솟는 화염, 열기 아지랑이, 연기
- **작열하는 주황·적색 광량이 씬 전체를 채움** — 지금까지와 반대로 매우 밝다
- 파티클: 위로 떠오르는 불티(ember)

### 6-2. 팔레트 초안
```js
const HEL = {
  rock: 0x2a2426, rockB: 0x1c1719, rockC: 0x3a3234,   // 흑요석/현무암
  ash:  0x4a4244,
  lava: 0xff5a1e, lavaBright: 0xffb24a, lavaDeep: 0xc42a08,
  ember: 0xff8a3a, smoke: 0x554a48,
  crackGlow: 0xff7a2a,
  void: 0x0a0606,
};
```
> ⚠️ **3막의 교훈**: 이 값을 그대로 넣지 말 것. 화면 밝기는 조명보다 **석재 반사율**이 지배한다.
> 3막처럼 `HEL_LIT` 같은 배수를 처음부터 두고, 1~3막과 같은 방식(적 없는 화면을 위아래 4구간으로
> 나눈 평균 휘도)으로 재서 맞출 것. 참고값: 1막 [25,39,35,25] · 2막 [27,44,41,31] · 3막 [39,59,55,40].
> 4막은 의도적으로 이보다 **높아야** 하는 유일한 막이다.

### 6-3. 구현 주의점
- **광량 반전**: 환경광을 확 올린다(예: `0x6a3a2a`, 강도 0.9~1.1). 안개는 붉은 톤 + 농도 낮춤.
- **용암은 광원이 아니라 발광 재질 + 소수의 큰 PointLight**로.
  점광원 상한은 이미 있다(`cullSceneLights` / `MAX_DYN_LIGHTS = 6`). **새 상한을 만들지 말 것** —
  두 시스템이 서로의 `visible`을 덮어써 실제 기기에서 흰 화면으로 터진 적이 있다.
- 용암 지대는 **지형 대미지 장판**으로 활용 가능(기존 `firePools` 재사용).
- 열기 아지랑이는 후처리 대신 **반투명 왜곡 빌보드 몇 장**으로 근사.
- 블룸 임계(`uThresh`)를 4막 값으로 따로 정할 것. 밝은 막이라 0.62면 화면이 통째로 번질 수 있다.

### 6-4. 몬스터 라인업 (제안 — 미확정)
| 층 | 타입 | 역할 |
|---|---|---|
| 91~ | **화염 임프** | 근접, 빠름, 죽을 때 폭발 |
| 95~ | **재의 궁수 / 화염술사** | 원거리, 착탄 지점에 화염 장판 |
| 100~ | **용암 골렘** | 근접 탱커, 균열에서 용암 분출 |
| 110~ | **불의 사냥개** | 돌진 특화, 무리로 등장 |

> 3막에서 확인된 배치 규칙: **근접 기본(막 시작) → 원거리(+4층) → 단단한 근접(+10층) →
> 느린 거인(+17층)**. 느린 거인류는 방당 수를 반드시 묶을 것.

### 6-5. 보스 (제안 — 미확정)
| 층 | 보스 | 컨셉 |
|---|---|---|
| 100 | **용암 거수** | 지면 갈라기 + 용암 분출 장판전 |
| 110 | **화염 처형자** | 거대 무기 근접, 패턴 읽기형 (2막 흑기사의 지옥판) |
| 120 | **지옥의 군주** | 4막 최종. **가짜 클라이맥스** — 격파 후 바닥이 갈라지며 심연으로 떨어지는 연출로 5막 진입 |

> ⚠️ **서사 지시**: 4막 최종 보스를 "모든 악의 근원"처럼 연출하되, 마지막에 **열린 마무리**를
> 남겨 5막 진입이 자연스럽게 이어지도록 한다.
> 3막에서 접었던 **더블 보스**(무적 코어 + 전투 대상) 구조는 화면이 트인 4막에서 다시 검토할 만하다.

---

## 7. 5막 — 심연 / 텅 빈 결핍 (121~150층) ❌ 미착수

> **공포 축**: **감각의 박탈**. 4막 지옥의 정반대. 지옥에서 타오르다 여기 오면 **정적이 더 무섭다.**

### 7-1. 비주얼 방향
- 벽도 천장도 보이지 않는 **거의 검은 보이드**
- **어둠 속에 떠 있는 부서진 돌 발판** — 비어 있어서 공간감이 깨진다
- 단 하나의 부자연스러운 색: **창백한 보라** 발광
- 파티클: 거의 없음

### 7-2. 팔레트 초안
```js
const ABY = {
  stone: 0x22202a, stoneB: 0x1a1822, stoneC: 0x2c2936,
  edge:  0x3a3548,
  violet: 0xb08ad8, violetDim: 0x6a4f8a, violetBright: 0xd9c2ff,
  void:  0x030308,
};
```

### 7-3. 구현 주의점 (5막은 구조가 다르다)
- **벽이 없다.** 바닥 타일 자체가 허공에 떠 있는 발판이고, 밖은 낙사/보이드.
  - 가장 안전한 구현: 격자 구조는 그대로 두되 **벽 메시를 생성하지 않고** 투명 충돌체만 등록.
    시각적으로는 발판이 끊긴 것처럼 가장자리를 깎는다.
  - ⚠️ 벽이 없으면 **SSAO 건축물 표식(`markArch`)이 걸릴 대상이 없다.** AO를 끄거나 발판에 표식을
    걸지 결정할 것.
  - ⚠️ 보스 몸집 대비 발판 폭을 flood fill로 검증할 것(3막 탐식 구울에서 실제로 문제가 됐다).
- **안개를 매우 진하게**, 색은 `void` → 멀리는 완전히 검게 사라짐.
- 환경광 최소(0.15~0.25), 플레이어 추적 광원 도달 거리도 줄여 압박감.
- 발판 가장자리에 **창백한 보라 발광 라인**(낙하 경계 인지 — 게임플레이 필수).

### 7-4. 몬스터 라인업 (제안 — 미확정)
| 층 | 타입 | 역할 |
|---|---|---|
| 121~ | **공허 유령** | 부유, 반투명, 소리 없이 접근 |
| 128~ | **관측자(눈)** | 원거리, 시선이 닿으면 지속 피해 |
| 135~ | **무형체** | 근접, 피격 시 분열 |
| 142~ | **심연의 파수꾼** | 대형 근접, 공간을 접어 순간이동 |

### 7-5. 보스 (제안 — 미확정)
| 층 | 보스 | 컨셉 |
|---|---|---|
| 130 | **공허의 아귀** | 발판을 파괴하며 전장 자체를 좁힘 |
| 140 | **첫 번째 것** | 1~4막 보스들의 실루엣을 흉내 내는 형태 모방 패턴 |
| 150 | **근원 (The Origin)** | **게임 최종 보스**. 다단계 페이즈 |

---

## 7-6. 지형 고저차 (진행 중 — v760에서 1·2단계)

던전 바닥을 평면 하나로 두지 않고 높이를 주는 작업. 4·5막(특히 발판이 떠 있는 5막)에 필요하다.

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 시각적 굴곡 — 바닥·벽이 부드럽게 오르내림 | ✅ v760 (`?relief=N`, 기본 0.9) |
| 2 | 배관 — 지면에 닿는 것들이 `groundHeightAt()`을 쓰게 | ✅ v760 |
| 3 | 계단식 고저차 — 격자에 정수 단, 이동 규칙·경사로 | ❌ |
| 4 | 적 AI·이펙트를 높이 인식으로 | ❌ |

**2단계의 합격 기준은 "`?relief=0`에서 화면이 v759와 픽셀 단위로 같다"** 였고 1·2·3막에서 확인했다.
높이 필드는 층 번호로 시드한 자체 난수를 쓰므로 `?relief` 값을 바꿔도 **던전 배치는 동일하다** —
지형만 갈아 끼워 비교할 수 있다.

**3단계 설계(권장)**: 연속 경사 대신 **격자에 정수 높이**(0/1/2, 한 단 ≈ 1.2)를 준다.
- 이동은 ±1단까지만, 2단 이상은 벽처럼 막는다 → 기존 XZ 충돌 로직을 거의 그대로 쓴다
- 지정 타일에만 경사로, 나머지는 옹벽(단차 옆면)
- 연속 경사의 난제(경사면 물리·미끄러짐)를 피하면서 고저차의 재미(내려다보는 방, 위에서 쏘는
  궁수, 낙차 있는 보스방)는 얻는다. 계단식이라 픽셀아트 룩에도 맞는다

**3단계에서 반드시 해야 할 것**
- **단차 옆면(옹벽) 메시.** 지금은 타일을 통째로 올리기만 해서 윗면이 수평 그대로다 — 음영이
  안 바뀌어 눈에 잘 안 띈다(진폭 0.9에서 화면 변화 최대 6.6/255). 옆면이 보여야 고저차로 읽힌다.
- **타일 두께 또는 옆면.** 이웃 타일 높이차가 진폭의 약 0.165배인데 타일 두께가 0.4라
  진폭 1.5를 넘으면 사이가 벌어진다.
- **이펙트 107군데**(장판·충격파·화살비·동전·데칼)를 `groundHeightAt`으로 돌리기
- **적 경로**: 지금은 XZ만 본다. 2단 차이를 벽으로 취급하는 판정 추가
- **5막**은 이 위에 '발판 밖 = 낙하'를 얹으면 된다(7-3절)

---

## 8. 밸런스 테이블

### 8-1. 막별 몬스터 강화 (구현됨, 자동 적용)
```js
const actIdx = Math.floor((player.floor - 1) / 30);
hpMul  = 1 + actIdx * 0.6;   // 1막 ×1.0 / 2막 ×1.6 / 3막 ×2.2 / 4막 ×2.8 / 5막 ×3.4
dmgMul = 1 + actIdx * 0.4;   // 1막 ×1.0 / 2막 ×1.4 / 3막 ×1.8 / 4막 ×2.2 / 5막 ×2.6
expMul = 1 + actIdx * 0.4;
```
> 초판의 "3막 hp ×2.2가 과한가"는 **손대지 않은 채로 출시됐다**(계수를 재보정한 적 없음).
> 즉 검증이 끝난 것이 아니라 **미확인 상태로 남아 있는 것**이다. 4막 밸런싱 때 3막과 함께
> 실측할 것 — ×2.8은 특히 확인이 필요하다.

### 8-2. 막별 시작 방 개수 / 맵 크기
막 안에서 4층마다 방 +1(최대 11), 맵 +2칸. 막이 바뀌면 **다시 작게 시작**.

| 막 | 시작 방 | 시작 맵 | 30층째 | 상태 |
|---|---|---|---|---|
| 1막 | 3 | 18×18 | 방 10, 34×34 | ✅ |
| 2막 | 4 | 18×18 | 방 11, 34×34 | ✅ |
| 3막 | 5 | 18×18 | 방 11, 34×34 | ✅ |
| 4막 | 5 | 20×20 | 방 11, 36×36 | 계획 |
| 5막 | 5 | 20×20 | 방 11, 36×36 | 계획 |

### 8-3. 보스 층
막마다 3회: `막시작+9`, `막시작+19`, `막시작+29`
1막 10/20/30 · 2막 40/50/60 · 3막 70/80/90 · 4막 100/110/120 · 5막 130/140/**150(최종)**

---

## 9. 쉼터 변형 규칙

정체성은 5개 모두 고정 — 관통하는 V자 길, 양쪽 동굴 입구, 시장 막사, 따뜻한 랜턴과 모닥불,
NPC(성직자·대장장이·여행자·음유시인·스킬 교관). 바뀌는 것은 **직전 막의 흔적**뿐.

| 쉼터 | 직전 막 | 묻어나는 흔적 | 색 충돌 | 상태 |
|---|---|---|---|---|
| 1–2 사이 | 동굴 | 무성한 초록, 생기 | 없음 (가장 안전) | ✅ |
| 2–3 사이 | 폐허 성 | 석재·아치 잔해, 빛바랜 천 | 금색이 살짝 바램 | ✅ |
| 3–4 사이 | 부패 | 암벽 가장자리로 번져오는 생체발광, 불 가까이 붙은 여행자들 | 따뜻함 vs 부패 보라·담즙 녹 | ⚠️ 현재는 "막힘" 안내만 |
| 4–5 사이 | 지옥 | 그을린 바위, 균열의 잔불, 연기에 그을린 막사 | 따뜻함 + 잔불 적색 | 계획 |

> **3–4 사이 쉼터가 가장 불안한 톤이어야 한다.** 모닥불 빛이 부패를 **겨우 밀어내는** 구도.
> 현재는 `restIsFinal` 분기로 출구가 붉게 막히고 길잡이 NPC가 안내만 한다.
> 4막 착수 시 이 분기를 풀면서 3–4 쉼터 변형을 같이 넣을 것.

각 쉼터는 `buildRestScene()`에 막 파라미터를 받아 팔레트/장식만 갈아끼우는 방식으로 구현할 것
(구조 재작성 금지).

---

## 10. 원화 프롬프트 (에셋 제작용)

스타일 토큰 공통: `isometric quarter-view 45°`, `low-poly faceted + hand-painted anime illustration`,
`--style raw`, `--stylize 250~300`.

**4막 지옥 배경**
```
isometric low-poly 3D game dungeon interior concept art, top-down quarter view 45 degree angle, infernal hellscape cavern, rivers and pools of glowing molten lava, jagged blackened obsidian rock formations and basalt pillars, cracked scorched ground glowing with embers, towering flames and rising heat haze, brimstone and smoke, blazing intense orange and red light flooding the scene, overwhelming heat and sensory overload, a hostile burning realm, flat-shaded faceted low-poly geometry blended with hand-painted anime illustration style, fiery palette of molten orange searing red and charcoal black, intense glowing high-contrast lighting, painterly sketch texture --ar 16:9 --style raw --stylize 250
```

**5막 심연 배경**
```
isometric low-poly 3D game dungeon interior concept art, top-down quarter view 45 degree angle, the abyss, a near-black endless void with no visible walls or ceiling, faint fragments of broken floating stone platforms suspended in darkness, almost total absence of light color and sound, profound cold emptiness and silence, a single unnatural pale violet glow as the only color in the void, distant unknowable presence, scale that feels infinite and incomprehensible, flat-shaded faceted low-poly geometry blended with hand-painted anime illustration style, near-monochrome palette of void black with a single pale violet accent, minimal sparse lighting, painterly sketch texture --ar 16:9 --style raw --stylize 250
```

**몬스터 캐릭터 시트 규칙**
- SD(치비) 비율 + 턴어라운드 뷰
- `cute` 제거하고 **`grotesque creepy [타입]`을 비율 토큰 바로 뒤에** 배치
- `not cute` 명시적 부정
- 얼굴 없는 구조물은 부정을 **여러 번 반복**(`no face no eyes no mouth`)
- 생체 갑옷은 `NOT metal NOT mechanical NOT hard plate` + `wet soft glistening biological tissue`
- 더블 보스는 `two clearly separate distinct figures`, `clear empty space between them` + `--stylize 280`

> **원화 색을 그대로 넣지 말 것.** 이 게임의 조명은 베이스 `0x60` 위가 사실상 평평해서
> 밝은 색을 넣으면 화면에서 회색으로 뭉갠다. `viewers/README.md`의 역산 절차를 따를 것.

---

## 11. 권장 작업 순서 (마일스톤)

- ~~**M0. 일반화**~~ → 부분 완료. **남은 3건(3-1·3-2·3-4)을 4막 첫 커밋으로.**
- ~~**M1. 3막 던전 테마**~~ ✅
- ~~**M2. 3막 몬스터**~~ ✅
- ~~**M3. 3막 보스 3종**~~ ✅ (2막 보스 3종도 함께 정리됨)
- **M4. 3–4 쉼터 변형 + 마을 포탈에 4막 추가** ← 다음
- **M5. 4막 지옥** (테마 → 몬스터 → 보스)
- **M6. 5막 심연** (발판/낙하 시스템 선행)
- **M7. 최종 보스 + 엔딩 처리**

---

## 12. 결정 필요 항목

1. **4·5막 몬스터/보스 이름과 컨셉** — 본 문서의 제안은 미확정. 원화 작업과 함께 확정 필요.
2. **5막 낙하 시스템** — 발판 밖으로 떨어지면 즉사인지, 피해를 입고 되돌아오는지.
3. **150층 이후** — 엔딩으로 끝낼지, 무한 모드/회차 계승(NG+)을 둘지.
4. **4막 hp 배율** — ×2.8이 과한지 실플레이 확인 후 조정(3막 ×2.2는 그대로 두기로 결론).
5. ~~**더블 보스 재도전 여부**~~ — v759에서 '패턴 없는 심장 + 부활' 형태로 확정. 같은 구조를 4·5막에도 쓸지는 열어 둔다.
6. **CrazyGames 제출과 4막 착수 순서** — 제출 안정화를 먼저 할지, 콘텐츠를 먼저 늘릴지.

---

*문서 기준: `index.html` v759 (3막 완료 · 90층 더블 보스 확정)*
*작업 규칙·렌더 파이프라인·함정 목록은 `CLAUDE.md`가 최신이다.*
