# 인수인계 — Dungeon Legion

> **이 문서의 목적**: 작업을 이어받는 사람이 **지금 어디까지 왔고 무엇이 열려 있는지** 를
> 파악하는 것. 게임 기획(무엇을 만드는가)과 저장소 규칙(어떻게 만드는가)은 각각 별도
> 문서에 있으므로 여기서는 반복하지 않고 **가리키기만** 한다.
>
> **최종 갱신**: `main` = v783(라이브) / 작업 브랜치 `claude/new-session-idx7n3` = **v785**(1막 벽 높이 복구 + 계단 받침 축소, 머지 대기)

---

## 1. 3분 요약

- **무엇**: Three.js r128 아이소메트릭 던전 크롤러 RPG. **`index.html` 단일 파일**(약 2.2MB)이 게임 전부다.
- **어디**: GitHub `PeaceZEKE/my-dungeon`. GitHub Pages가 `main`을 배포하므로 **`main`에 올라가는 순간 라이브**다.
- **상태**: 1~3막(1~90층) 구현 완료. 4·5막 미착수. CrazyGames 제출 준비 중.
- **직전 작업**: 검을 휘두를 때 남는 **스미어 잔상(픽셀아트 잔상 띠)**. v768에 도입해 v783까지
  16번 고쳤고, 지금은 **2막 기사 보스(흑기사, 50층)** 에 붙어 있다. **v783까지 `main`에 머지돼
  라이브다.**
- **지금 막힌 곳**: 잔상 크기에 대한 사용자 판단 대기. 아래 4절.

---

## 2. 문서 지도 — 이 순서로 읽을 것

| 순서 | 문서 | 내용 | 분량 |
|---|---|---|---|
| 1 | **`CLAUDE.md`** | *어떻게 만드는가.* 저장소 규칙·릴리스 절차·렌더 파이프라인·**반복해서 발목 잡은 함정 모음**. 충돌하면 이 문서가 최신이다. | 매우 김. 전부 읽을 것 |
| 2 | **`기획서.md`** | *무엇을 만드는가.* 막별 컨셉·몬스터 라인업·밸런스 표·4·5막 계획·코드 앵커. | 김 |
| 3 | 이 문서 | 지금 진행 중인 작업의 상태와 열린 결정 | 짧음 |
| 4 | `viewers/README.md` | 캐릭터 모델링 워크플로 | 필요할 때 |

**`CLAUDE.md`의 '반복해서 발목을 잡은 함정들' 절은 건너뛰지 말 것.** 대부분이 실제로 한 번씩
사고를 낸 뒤 적힌 것이고, 같은 실수를 다시 하기 매우 쉽다(TDZ · three 프로그램 캐시 ·
`Math.random` 시드 시점 · 노멀 패스 표식 · 블룸 임계가 막마다 다른 것 등).

---

## 3. 절대 어기면 안 되는 것 (사용자 지시)

`CLAUDE.md`에 전부 있지만, 이것만은 여기에도 적어 둔다.

1. **`main`에 임의로 올리지 말 것.** 작업은 `claude/…` 브랜치에서 하고, 검증 결과를 보고한 뒤
   **사용자가 머지하라고 할 때만** `main`에 반영한다. 문서 변경도 예외 없다.
2. **코드를 바꿨으면 `GAME_VERSION`(4155줄 부근)을 반드시 같이 올린다.** 좌측 상단 배지로
   사용자가 배포를 확인한다. **문서만 고쳤을 때는 올리지 않는다.**
3. **게임 파일은 항상 두 벌로 보낸다** — `index.html`(배포용) + `index-vNNN.html`(보관용).
   내용은 같고 이름만 다르다. **코드를 바꿨으면 매번 보낸다**(전에 안 보내서 지적받았다).
4. **새 기능은 URL 플래그로 끌 수 있게** 만들되, 보내는 파일에는 **켜진 상태로** 넣는다.
   사용자가 모바일에서 URL 파라미터 없이 바로 확인할 수 있어야 한다.
5. 머지 전 문법 검사 — 마지막 `<script>` 블록만 잘라 `node --check`(`CLAUDE.md` 릴리스 절차 참고).
6. 임시 디버그 스크립트를 저장소에 만들지 말 것(스크래치패드를 쓴다).
7. CrazyGames 준수사항(광고 중 음소거 · 일반 접속에서 치트 버튼 숨김 · 미드게임 광고 하한 2분)을
   되돌리지 말 것.

사용자는 한국어로 소통하고 코드 주석도 한국어다. 모바일 작업이 많아서, 파일을 주고받기보다
**저장소에 올려 두는 쪽**을 편해한다.

---

## 4. 지금 열려 있는 결정 ⚠️

**잔상(리본)의 크기를 어떻게 할지 사용자 답을 기다리는 중이다.**

기준이 둘인데 리그마다 서로 어긋난다:

| 항목 | 기사(플레이어) | 흑기사(보스) |
|---|---|---|
| 무기 리치 / 키 | 0.91 | **1.73** |
| 띠 화면 크기(최대) | 72×52px | **280×189px** |
| 같은 화면에서 캐릭터 높이 | 45px | 약 105px(3.0유닛 기준 78~80px) |

- **무기 리치에 맞추면**(현재 v783) 밝은 머리가 칼날 위에 얹혀 "칼이 지나간 자리"로 읽힌다.
  대신 대검 보스라 띠가 몸보다 크다(최대 280×189px · 보스 78~80px).
- **몸 높이에 맞추면**(v781에서 시도) 띠가 손 안쪽으로 들어가 **손잡이 옆에서 시작**하고,
  사용자가 "꼬리만 보이고 시작 부분이 안 보인다"고 지적했다. 되돌린 이유가 이것이다.

**줄일 수는 있으나 하한이 있다** — `makeSmearTrail(scale)`의 `scale`을 낮추되 **띠 안쪽이 손보다
안으로 들어가지 않는 선까지만**(흑기사는 `scale` 0.81이 하한). 그 아래로 내리면 위 증상이 그대로
재발한다.

사용자가 "크다"고 하면 `scale`을 0.81~1.0 사이에서 조정하고, "괜찮다"고 하면 **다른 보스들에도
같은 방식으로 붙이는 것**이 다음 작업이다(사용자가 "보스몹들에만 넣자"고 복수형으로 말했다).

---

## 5. 브랜치 상태

```
origin/main                    v783   ← 라이브
claude/new-session-idx7n3      v785   ← 1막 벽 높이 복구 + 계단 받침 축소(머지 대기)
```

**작업 브랜치가 한 발 앞서 있다.** 다음 작업은 작업 브랜치에서 시작하고, 검증 결과를 보고한 뒤
**사용자가 머지하라고 할 때만** `main`에 반영한다. 머지 전에는 반드시 문법 검사를 통과시킬 것
(마지막 `<script>` 블록만 잘라 `node --check` — `CLAUDE.md` 릴리스 절차).

---

## 6. 스미어 잔상 — v768~v783 경위

### 6-1. 이게 뭔가

검을 휘두를 때 칼이 지나간 자리에 남는 픽셀아트풍 잔상 띠. 어깨 피벗 → 손 방향으로
**어깨 높이의 수평 부채꼴**을 깔고, 최근 0.11초의 궤적을 삼각형 띠로 잇는다.

핵심 설계 제약 세 가지(전부 이 파이프라인 때문이다):

- **반투명·알파를 쓸 수 없다.** 포스트가 색을 양자화해서 중간톤이 될 뿐이고, 반투명 재질은
  노멀 패스에서 빠져 **검은 외곽선을 못 받는다**. 그래서 불투명 + `toneMapped:false` + 정점색이다.
  "옅어지며 사라지는" 느낌은 색이 아니라 **디더 구멍**으로 낸다.
- **정점은 CPU에서 갱신한다.** 정점 셰이더로 흔들면 색 패스만 움직이고 노멀·그림자 패스는
  제자리라 외곽선이 형상과 어긋난다.
- **재질·메시는 첫 스윙 때 만든다.** 모듈 로드 시점에 만들면 `generateUUID`가 `Math.random`을
  먹어 층 생성 난수 스트림이 통째로 어긋난다.

### 6-2. 버전별로 무엇을 왜 바꿨나

| 버전 | 바꾼 것 | 이유 |
|---|---|---|
| v768 | 도입 | 뷰어(`viewers/smear_ribbon_spike_v1.html`)에서 만들어 옮김 |
| v770 | 검신 → **어깨→손 수평 부채꼴** | 검신으로 띠를 만들면 세로 커튼이 돼 옆에서 보면 선이 된다 |
| v771 | 가장자리 계단 양자화 + 흩어지는 알갱이 | 레퍼런스의 "픽셀아트 느낌"의 실체 |
| v772 | 청록 → **검 날과 같은 회색** | 사용자 지시. 재질 색이 아니라 **화면에 찍히는 색**(#919184)에 맞춤 |
| v773→v774 | 호를 가로지르는 3겹 → **호를 따라** 나누기 | 사용자 지시. 3겹은 "칼이 지나간 자리"가 아니라 무늬 고리로 읽힌다. **다시 시도하지 말 것** |
| v775 | 3단 → **5단 + 꼬리 디더 소산** | 사용자 지시("투명도가 옅어지는 것처럼") |
| v776 | 디더 시작점을 톤 함수의 역으로 계산 | 사용자 지시("둘째 단부터"). 상수로 박아 두니 실제로는 셋째 단부터였다 |
| v777 | 디더 칸 4×2 → **8×4** | 사용자 지시. 칸이 화면에서 1~2px이어야 픽셀 디더로 읽힌다 |
| v778 | 띠 폭 절반 | 사용자 지시. **안쪽은 고정하고 바깥만** 당긴다(안쪽은 검에 붙은 쪽) |
| v779 | 호 반지름 0.68배 | 사용자 지시("세로폭"). 이 띠는 완전한 수평면이라 세로 두께가 없다 — 호를 줄이는 게 답 |
| v780 | 폭 방향에도 디더 | 사용자 지시. 마스크를 ASCII로 찍어 보니 조각이 전부 **높이 1px 가로 대시**였다 |
| **v781** | **플레이어에서 빼고 보스에** | 사용자 지시("캐릭터는 너무 작아서 잘 안 보이네"). 개체별 잔상으로 구조 변경 |
| v782 | 흑기사는 **마그마 주황** | 사용자 지시. 검의 주황 마그마 심에 맞춤 |
| **v783** | 크기 기준을 **팔 길이 → 무기 리치** | 사용자 지적("리본 시작 부분이 안 보인다"). 아래 6-4 |

### 6-3. 코드 구조 (v783 기준)

| 요소 | 줄 | 설명 |
|---|---|---|
| `SMEAR_ON` / `SMEAR_PLAYER` | 4222 부근 | `?smear=0`으로 끔 / `?smearp=1`로 플레이어에도 켬 |
| 상수 블록 | 13060~13135 | 반지름·팔레트·디더·알갱이. **주석에 근거 수치가 전부 적혀 있다** |
| `smearTrails` | 13136 | 살아 있는 잔상 전부 |
| `makeSmearTrail(scale, cols)` | 13143 | 개체마다 하나. `scale`=크기 조정, `cols`=팔레트 |
| `_smearReach(handObj, S, H)` | 13155 | 무기 리치 배수 측정(한 번만, 캐시) |
| `updateSmearTrail(t, dt, active, rootObj, shObj, handObj, armZ)` | 13187 | 표본 기록 + 지오메트리 생성 |
| `tickSmearTrails(dt)` | 13348 | **`update()` 첫머리(20025)에서 반드시 호출** |
| 플레이어 훅 | 20331 | 기본 꺼짐 |
| 흑기사 훅 | 21610 | **보스 블록 첫머리**에 있어야 한다 |

**주의 둘:**
- `tickSmearTrails`를 안 부르면 보스가 얼거나 죽었을 때 띠가 화면에 얼어붙어 남는다.
- 보스 훅은 **보스 블록 첫머리**에 넣어야 한다. 아래 분기들이 `continue`를 많이 해서
  뒤에 두면 그냥 빠진다.

### 6-4. v783에서 고친 것 — 여기가 제일 중요하다

사용자가 "꼬리만 보이고 리본 시작 부분이 안 보인다"고 했다.

**가려진 게 아니었다.** 톤 단별로 「실제 보이는 픽셀」과 「깊이검사를 끄고 그렸을 때」를 같이 재니
머리(가장 밝은 단)는 981px에 **가려짐 0%** 였다. 문제는 머리가 **칼날이 아니라 손잡이 옆**에
찍히고 있었다는 것.

원인은 반지름을 **팔 길이 배수**로 물려준 것이다. 무기가 긴 리그일수록 어긋난다:

| | 어깨→칼끝 | 띠가 덮은 범위 | 칼이 쓸고 간 자리 중 |
|---|---|---|---|
| 기사 | 팔의 2.89배 | ~1.76배 | 61% |
| 흑기사(v782) | 팔의 3.44배 | ~1.07배 | **31%** |

**띠는 팔이 아니라 칼이 지나간 자리를 그리는 것**이므로, 반지름을 **무기 리치 배수**로 바꿨다.
기사는 리치가 팔의 2.89배라 환산해도 예전 값과 1.5% 안에서 같다(회귀 없음을 실측 확인).

리치를 잴 때 두 가지가 걸렸다 — 둘 다 `_smearReach` 주석에 남겨 뒀다:
- **'팔 길이 + 칼 길이'로 더하면 안 된다.** 이 리그는 칼이 팔에 수직이라(칼날은 항상 월드 +Y)
  합이 실제보다 훨씬 길다(기사 1.75 ↔ 더하면 2.24). **어깨에서 직접** 잰다.
- **대기 자세로 재면 안 된다.** 흑기사는 휘두를 때 칼 각도가 바뀌어 어깨→칼끝이 5.41 ↔ 7.02로
  달라진다. 첫 표본(휘두르는 자세)에서 한 번 재서 캐시한다.

### 6-5. 현재 계측값

| 항목 | 플레이어(`?smearp=1`) | 흑기사(기본 켜짐) |
|---|---|---|
| 삼각형 최대 | 456 | 11,848 |
| 드로우콜 | +1 | +1 |
| 한 스윙에 보이는 프레임 | 92(연속 스윙) | 17 |
| 띠 화면 크기 | 72×52px | 최대 280×189px |

기본 접속에서 플레이어 잔상은 **객체 자체가 생성되지 않는다**. 5·25·50·75층 전환 콘솔 에러 없음.

---

## 7. 다음 작업 후보

1. **(대기 중)** 잔상 크기 확정 → 4절.
2. 확정되면 **다른 보스들에도 잔상 적용**. 붙일 때 반드시 확인할 것:
   - 팔레트는 **그 보스 무기의 화면 색**으로 다시 잰다. 흑기사 팔레트(`SMEAR_COLS_DK`)는
     머리 톤 휘도가 0.632라 **2막 임계(0.70)는 안 넘지만 1·3막 임계(0.62)는 넘는다** —
     그대로 쓰면 둥근 빛덩이로 번진다.
   - 이 띠는 **수평면**이라 위에서 내려찍는 공격에는 각도가 안 맞는다. 횡베기형 패턴에만 붙일 것.
   - 무기에 색이 둘이면(날 + 발광 심) **발광 심이 아니라 본체**에 맞춘다. 띠는 면적이 넓어서
     발광 밝기로 칠하면 흰 덩어리가 된다.
3. 그 뒤 큰 그림: **4막(91~120층, 지옥)** 착수. `기획서.md` 3절의 "4막 착수 전 반드시 고칠
   하드코딩"(`currentAct()` · `descend()` · `actBaseRooms`)부터 푸는 것이 시작이다.

---

## 8. 계측 하네스 — 이렇게 재야 결론이 맞는다

`CLAUDE.md`의 '렌더 변경을 비교할 때' 절이 원칙이고, 여기는 **이번 작업에서 실제로 쓴 방법**이다.
스크래치패드의 스크립트는 세션이 끝나면 사라지므로 필요하면 다시 만들 것.

**기본 뼈대**

```bash
# 반드시 저장소 루트에서 띄우고, 버전부터 확인한다(예전 파일이 서빙되면 한참 헤맨다)
python3 -m http.server 8899
curl -s localhost:8899/index.html | grep -o "GAME_VERSION = 'v[0-9]*'" | head -1
```

```js
// 부팅보다 먼저 걸어야 한다 — page.evaluate 안에서 걸면 이미 늦다
await page.addInitScript(() => {
  let s = 20260901 >>> 0;
  Math.random = () => { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; };
  window.__t = 0;                                  // 시계도 고정해야 대조군이 0이 나온다
  performance.now = () => window.__t;
  Date.now = () => 1767225600000 + window.__t;
});
// 이후 매 프레임: window.__t += 1000/60; update(1/60);
// rAF는 끊고 renderWithPost(scene)을 직접 부른다
```

**보스 하네스에서 걸린 것들**

- `damagePlayer = () => {};` 로 막지 않으면 플레이어가 죽어 게임오버 오버레이가 덮는다.
- 보스 위치를 **매 프레임 고정**해야 프레임 간 크롭이 안 흔들린다.
- 층 장식(`floorGroup`에서 `bb.max.y > 0.3`)과 다른 적을 숨겨야 개체가 보인다.
- 흑기사 횡베기 강제 시작:
  ```js
  boss.rockState='none'; boss.roarState='none'; boss.rushState='none';
  boss.riftState='none'; boss.recoverState='none';
  boss.sweepState='telegraph'; boss.sweepTimer=0.5; boss.sweepDir=1;
  ```

**세 가지 측정 기법 — 이번 작업의 결론은 전부 여기서 나왔다**

| 기법 | 어떻게 | 무엇을 알 수 있나 |
|---|---|---|
| **가시성 차이 마스크** | 한 프레임 찍고 → `mesh.visible=false` → 다시 찍어 달라진 픽셀만 모음 | 특정 메시가 화면에 **실제로** 내는 색·픽셀 수. 팔레트를 고를 때 쓴다 |
| **깊이검사 on/off 비교** | 같은 마스크를 `material.depthTest` 켜고/끄고 두 번 | **가려진 것인지 안 그려진 것인지** 갈린다. v783 진단의 핵심 |
| **톤 색 갈아 끼우기** | 팔레트를 빨강·주황·노랑·초록·파랑으로 바꿔 한 프레임 | 어느 단이 화면 어디에 찍히는지. 마스크·수치보다 빠른 유일한 경우 |

**하지 말 것**

- 스크린샷 한 장으로 3~17프레임짜리 동작을 판단하지 말 것. 여러 프레임을 한 장에 붙여 본다.
- 크롭이 이펙트를 자르고 있는지 먼저 확인할 것(**실제로 잘린 크롭을 보고 "너무 크다"고
  잘못 판단한 적이 있다**). 화면 경계 상자를 먼저 재고 크롭을 정한다.
- 픽셀 배열을 페이지 밖으로 옮기지 말 것(장당 2.2MB). 페이지 안에서 해시까지 내고 해시만 비교한다.
- **빌드가 다르면 난수 스트림이 어긋나 픽셀 A/B가 원천적으로 불가능하다**(THREE 객체 생성이
  `Math.random`을 먹는다). 객체 수가 달라지는 변경은 스크린샷 비교 대신 **기능을 직접 계측**할 것.

---

## 9. 이번 작업에서 일반화된 교훈

전부 `CLAUDE.md`에 반영해 뒀다(스미어 절). 요약만:

- **이펙트 색은 재질 색이 아니라 '화면에 찍히는 색'에 맞춘다.** 조명·포스트를 거치면 완전히 달라진다.
- **이펙트 크기의 기준은 몸이 아니라 그 이펙트가 표현하는 것**(칼자국이면 무기 리치)이다.
- **리그가 다르면 상수를 그대로 물려주면 안 된다.** 팔/키 비, 무기/팔 비가 리그마다 다르다.
  상대값(리치 배수, 폭 대비 칸 수)으로 두면 리그가 바뀌어도 따라온다.
- **"안 보인다"는 세 가지로 갈린다** — 안 그려짐 / 가려짐 / 엉뚱한 자리에 그려짐.
  깊이검사 on/off와 색 갈아 끼우기로 갈라야 한다. 추측으로 고치면 엉뚱한 것을 건드린다.
- **재현 못 한 것을 추측으로 고치지 말 것**(`CLAUDE.md`에 v761 사례가 있다).
